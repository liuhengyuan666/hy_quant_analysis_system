# V7 设计规划：Market Evolution Semantic Layer

> 本文件用于持续记录 V7 阶段的设计规划、技术决策与实施方向。
> 创建日期：2026-07-08
> 前置阶段：V6（Research Layer v1 + Reporting Platform）已落地，V6 Reporting Platform 已冻结
> **当前状态：V7.2B 已完成实现；V7.2C Research Calibration 已实现；Phase B+ Calibration Baseline Freeze 已完成；V7.3 Research Synthesis Layer 已实现（Evidence Aggregation + Research Consensus CLI）；V7.3.1 Consensus Stabilization 已实现；ADR-077 Research Platform 1.0 正式冻结**
>
> **V7.3 定义（已批准）**：Research Consensus 是 Observation / Evolution / Historical Evidence 的综合解释器，不是策略决策器。采用 Evidence Aggregation（证据聚合）而非硬编码规则树，输出保持研究语言（Bias、Confidence、Supporting Evidence、Contradictions），不输出买卖建议或仓位建议。

---

## 1. 目标概述

V6 已经完成的是：

> **Observation Layer — 描述市场当前发生了什么。**

V7 的核心目标是：

> **Market Evolution Semantic Layer — 描述市场正在朝哪里演化，并提供历史证据与最终研究综合。**

完成从：
```
Observation（市场当前状态）
```
到：
```
Observation → Market Evolution → Historical Evidence → Research Synthesis
```
的语义层扩展。

这是 Research Layer 从**静态描述**走向**动态演化理解**的关键一步。

---

## 2. 核心架构边界

### 2.1 V7 四层架构

```text
Observation Layer（V6 已完成，V7 不改动）【Research Platform 1.0 已冻结】
├── Environment
├── Signal
├── Stretch
└── Rotation

Market Evolution Layer（V7.1，已冻结）【Research Platform 1.0 已冻结】
├── Confirmation        — 市场有没有确认？
└── Recovery              — 市场恢复了多少？

Historical Evidence Layer（V7.2）【Research Platform 1.0 已冻结】
├── V7.2A Market Fingerprint Foundation
│   └── MarketFingerprint / ObservationVector / EvolutionVector / Builder
├── V7.2B Evidence Retrieval Engine
│   ├── Normalizer        — 特征归一化
│   ├── DistanceMetric    — 可替换距离函数
│   ├── SimilarityMatcher — 相似度匹配
│   ├── OutcomeProfiler   — 前向收益与风险统计
│   └── CLI research analogues
└── V7.2C Research Calibration
    ├── Phase A：Evidence Layer Freeze Review（已完成）
    └── Phase B：真实市场行为验证与阈值校准（已实现）
    └── Phase B+：Calibration Baseline Freeze（已完成）
        └── 建立版本化校准基线、累积重复运行、检测漂移

------------ Evidence Layer Freeze & Calibration Baseline Freeze ------------

Research Synthesis Layer（V7.3，已启动；V7.3.1 已稳定化）【Research Platform 1.0 已冻结】
└── Consensus             — 研究层综合判断（含 Transition 解释）
    └── V7.3.1：ConsensusConfig、版本化 ConsensusSummary、Calibration Baseline Version 常量、集成测试

>>> Research Platform 1.0 Freeze <<<
>>> 后续新增市场观测/指标/内容属于 Research Content Evolution，
>>> 只允许通过现有层消费，不再修改 Semantic Architecture。
```

> **关键边界**：V7.2A/V7.2B Evidence Layer 已在 Phase A 冻结；V7.2C Phase B 已完成校准框架实现；Phase B+ 完成后建立版本化 Calibration Baseline 并进入冻结。V7.3 Consensus 只能消费 Evidence，不能修改 Evidence 契约，且只能在 Calibration Baseline 冻结后启动。**ADR-077 已正式冻结 Research Platform 1.0（Observation + Evolution + Evidence + Consensus）。未来新增市场内容必须作为 Research Content Evolution 输入现有层，不允许修改 Semantic Architecture。**

### 2.2 关键架构判断

#### 判断 1：Transition 不进入 ResearchContext，而是 Consensus 的一部分

Transition 不是独立的跨消费者语义。它是系统基于 Recovery、Confirmation、历史轨迹给出的最终解释。如果 Transition 作为独立字段，可能出现：

```
Recovery = 18（Weak）
Confirmation = Weak
Transition = Expansion（矛盾）
```

因此：
- `Transition` 只存在于 `ConsensusSummary` 中
- `ResearchContext` 不增加 `TransitionSummary`

#### 判断 2：Recovery 在代码中叫 `RecoverySummary`，CLI 显示为 "Recovery Index"

`Index` 是用户-facing 名称，表示一个综合读数；`RecoverySummary` 是代码中的语义结构，包含 `score` 和 `drivers`。

输出示例：
```text
Recovery Index: 42
Drivers:
  - Breadth improving
  - Volatility shrinking
  - Drawdown recovering
```

#### 判断 3：Historical Analogues 不对外暴露原始相似度百分比

内部保留 distance / similarity 计算，但 CLI 输出使用等级或排名：

```text
Top Historical Matches
  #1  2026-05-18  Very High
  #2  2026-04-03  High
  #3  2026-03-12  Moderate

Forward 20D Outcome:
  Median:  +2.1%
  Mean:    +2.4%
  Best:    +8.7%
  Worst:   -3.2%
  Win Rate: 80%
```

原因：相似度数字依赖具体距离函数和特征权重，不同实现会变化，直接暴露百分比会误导用户。

#### 判断 4：Rotation 不独立为一级模块，而是升级现有 `RotationSummary`

不新建 `RotationFlowSummary` 或 `RotationFlowReport`。在 `ResearchContext::RotationSummary` 中新增字段：

```rust
struct RotationSummary {
    rotation_state: String,          // 已有：broad / concentrated
    leadership_stability: f64,       // 已有
    leadership_transition: String,  // 新增：leader 是否切换
    rotation_acceleration: f64,       // 新增：轮动速度
    theme_dispersion: f64,          // 新增：主题分散度
}
```

### 2.3 继续遵守 V6 四层架构所有权

```text
数据所有权（Data Ownership）
    ResearchDataset              ← app-service 内部，ephemeral，不暴露
    ResearchSnapshot             ← app-service 内部，computation workspace

语义所有权（Semantic Ownership）
    ResearchContext              ← 跨消费者共享的 canonical semantic contract

展示所有权（Presentation Ownership）
    ReportingSnapshot            ← 展示层 metadata + research context
    ReportInput                  ← 文档专属输入，document generation workflow 独占
    ReportBuilder                ← 文档组装（Research / Audit / Review）
    ReportDocument               ← 渲染前文档模型

渲染所有权（Rendering Ownership）
    Formatter                    ← Markdown / Text / JSON 渲染，无业务计算

消费者（Consumers）
    CLI / Desktop / API / GPT / Email / PDF
```

#### 核心规则

- `ResearchContext` ≠ 万能 DTO，只承载跨消费者共享的独立市场语义。
- `ReportInput` 只承载 document payload，不重复 metadata（scope/date/generated_at）。
- 所有可复用的研究计算位于 `core-domain::research`。
- `ResearchDataset` 永不暴露到 `app-service` 边界之外。
- Production Surface（`DashboardSnapshot`、`export-report`）保持冻结。
- V7 新增语义进入 `ResearchContext` 前必须证明是**跨消费者共享的独立市场语义**，而不是为了某个报告新增字段。

---

## 3. V7.1：Market Evolution Layer

### 3.1 Confirmation

**问题**：市场有没有确认？

**结构**：聚合为三个一级维度，避免六个维度平铺。

| 一级 | 二级证据 |
|------|----------|
| **Trend** | Trend score, Leadership, Rotation |
| **Participation** | Breadth, Volume expansion, Turnover coverage |
| **Risk** | Environment, Risk appetite, Volatility |

**输出示例**：
```text
Market Confirmation
  Trend:         ★★★★☆
  Participation: ★★★☆☆
  Risk:          ★★★★☆
  Overall:       Moderate
```

**实现位置**：
- 纯计算：`crates/core-domain/src/research/confirmation.rs`
- 语义契约：`crates/research-context/src/lib.rs` 新增 `ConfirmationSummary`
- 文档输入：`crates/report-builder/src/lib.rs` 新增 `ConfirmationReportInput`
- CLI 命令：`research confirmation`

**约束**：
- 只描述市场确认程度，不输出买卖建议
- 不进入任何决策链路
- 所有子维度必须附证据

### 3.2 Recovery

**问题**：市场恢复了多少？

**输出示例**：
```text
Recovery Index: 42
Drivers:
  - Breadth improving
  - Volatility shrinking
  - Drawdown recovering
```

**代码结构**：
```rust
struct RecoverySummary {
    score: f64,              // 用户-facing：Recovery Index
    drivers: Vec<String>,    // 驱动因素
}
```

**输入数据**：
- `MarketStateObservation.drawdown_pct`
- `EnvironmentSnapshot.breadth_5d_delta`
- `MarketStateObservation` 波动率相关字段
- 价格相对近期低点的修复幅度

**实现位置**：
- 纯计算：`crates/core-domain/src/research/recovery.rs`
- 语义契约：`crates/research-context/src/lib.rs` 新增 `RecoverySummary`
- 文档输入：`crates/report-builder/src/lib.rs` 新增 `RecoveryReportInput`
- CLI 命令：`research recovery`

**约束**：
- Recovery 只回答 "恢复多少"，不回答 "是否进入牛市"
- 不输出阶段标签（Stage 由 Consensus 中的 Transition 解释）

---

## 4. V7.2：Historical Evidence Layer

### 4.1 拆分：V7.2A 与 V7.2B

V7.2 拆分为两个阶段，以避免过早将相似度算法固化为 Market Fingerprint 的一部分。

```text
V7.2A：Market Fingerprint Foundation
├── MarketFingerprint          — 规范历史特征表示
├── MarketFingerprintBuilder   — 从 ResearchContext / DashboardSnapshot 构建指纹
└── Tests / Contract           — 稳定契约，不暴露算法

V7.2B：Evidence Retrieval Engine
├── normalize.rs              — 特征归一化
├── distance.rs               — DistanceMetric trait + 可替换实现
├── matcher.rs                 — SimilarityMatcher / find_similar + find_cluster
├── outcome.rs                 — OutcomeProfiler → OutcomeProfile
└── CLI research analogues     — 用户命令
```

**原则（ADR-072）**：
1. **Fingerprint Engine 不负责预测**：只负责 `Market State → Canonical Fingerprint → Historical Retrieval`。不输出 Probability / Bullish / Bearish。
2. **Matcher 不负责解释**：只输出 Match、Distance、Outcome；解释属于 Consensus。
3. **Distance Strategy 可替换**：通过 `DistanceMetric` trait 实现，可替换 Cosine / Euclidean / WeightedCosine / Mahalanobis 等，不修改 Fingerprint。
4. **Normalize 不属于 Distance**：`Builder → Fingerprint → Normalizer → Distance → Ranking`，Normalizer 独立。
5. **Similarity 不直接暴露百分比**：内部保留 distance，外部使用等级（Very High / High / Moderate / Weak）或排名（Rank #1）。
6. **OutcomeProfile 是独立对象**：`HistoricalMatch → OutcomeProfiler → OutcomeProfile`，支持 20D / 60D / 120D / MaxDD 等扩展。
7. **SearchResult 包含统计元信息**：返回 `matches`、`searched_days`、`filtered_days`、`average_distance` 等，丰富 Evidence。

### 4.2 V7.2A：Market Fingerprint Foundation

**新建 crate**：`crates/market-fingerprint-engine/`

**核心职责**：
- 输入：`ResearchContext` 或 `DashboardSnapshot`
- 输出：`MarketFingerprint`
- 不提供相似度搜索（V7.2B 再做）

**Market Fingerprint 结构**：
```rust
struct MarketFingerprint {
    pub scope: AnalysisScope,
    pub date: NaiveDate,
    pub observation: ObservationVector,
    pub evolution: EvolutionVector,
}

struct ObservationVector {
    pub environment: f64,
    pub signal: f64,
    pub stretch: f64,
    pub rotation: Vec<(String, f64)>,  // 主题/领涨结构
}

struct EvolutionVector {
    pub confirmation: f64,
    pub recovery: f64,
}
```

**设计原则**：
- Fingerprint 按 Semantic Layer 分组，未来新增 Consensus / Fear / Liquidity 等维度时，不需要修改整体结构
- Fingerprint 是核心产出，不是 Historical Analogues 的附属
- 未来 Timeline、Replay、Cluster、Pattern Search、Strategy Backtest 都可以复用

**Builder 设计**：
```rust
pub struct MarketFingerprintBuilder;

impl MarketFingerprintBuilder {
    pub fn build(research: &ResearchContext) -> MarketFingerprint { ... }
    // 未来可扩展：
    // pub fn build_from_dashboard(snapshot: &DashboardSnapshot) -> MarketFingerprint { ... }
}
```

**V7.2A 不实现**：
- `find_similar()` / `find_cluster()`
- 距离函数
- 归一化
- `OutcomeProfile`
- CLI `research analogues`

### 4.3 V7.2B：Evidence Retrieval Engine

**问题**：历史上什么时候出现过类似状态？这些状态之后发生了什么？

**实现管线**：
```text
ResearchContext
    ↓
MarketFingerprintBuilder
    ↓
MarketFingerprint
    ↓
Normalizer
    ↓
DistanceMetric (可替换)
    ↓
SimilarityMatcher
    ↓
HistoricalMatch
    ↓
OutcomeProfiler
    ↓
SearchResult
```

**模块结构**：
```text
crates/market-fingerprint-engine/src/
├── lib.rs
├── model.rs        # SearchResult, HistoricalMatch, MatchLevel
├── fingerprint.rs  # MarketFingerprint, ObservationVector, EvolutionVector
├── builder.rs      # MarketFingerprintBuilder
├── normalize.rs    # Normalizer trait + 实现
├── distance.rs     # DistanceMetric trait + Cosine/Euclidean/WeightedCosine
├── matcher.rs      # SimilarityMatcher: find_similar, find_cluster (stub)
└── outcome.rs      # OutcomeProfiler + OutcomeProfile
```

**输出示例**：
```text
Historical Evidence Search
  Searched days:        1250
  Filtered days:        23
  Average distance:     0.18

Top Matches
  #1  2026-05-18  Very High
  #2  2026-04-03  High
  #3  2026-03-12  Moderate
  #4  2026-02-20  Moderate
  #5  2026-01-15  Weak

Forward 20D Outcome (based on top 5 matches)
  Median:    +2.1%
  Mean:      +2.4%
  Best:      +8.7%
  Worst:     -3.2%
  Win Rate:  80%
  Median Max Drawdown: -4.1%
```

**约束**：
- 不对外暴露原始相似度百分比
- 使用等级：`Very High` / `High` / `Moderate` / `Weak` 或排名 `#1` / `#2` / `#3`
- 相似度计算内部保留，但知道它是距离函数的产物，会随算法变化
- `find_cluster()` 接口先冻结，V7.2B 中可返回 `unimplemented`

**CLI 命令**：`research analogues`（V7.2B 再开放）

### 4.4 V7.2C：Research Calibration

> **目标**：不是开发新功能，而是验证 V7.1 与 V7.2B 的输出在真实市场中是否行为稳定、语义合理。

V7.2C 分三阶段推进：

| 阶段 | 状态 | 目标 |
|------|------|------|
| Phase A：Evidence Layer Freeze Review | 已完成 | 审查并冻结 V7.2A/V7.2B 契约，形成 ADR-071 |
| Phase B：Calibration Framework | 已实现 | 构建可运行的校准报告框架，输出四章节 Markdown 报告 |
| Phase B+：Calibration Baseline Freeze | 已完成 | 为报告增加距离分布直方图与基线元数据，建立版本化基线，多次运行检测漂移 |

**问题**：
- `research confirmation` 的输出是否长期固定在一个等级，失去区分度？
- `research recovery` 是否对市场压力变化敏感？
- `research analogues` 的相似日匹配是否符合市场直觉？
- 前向收益统计是否稳定，还是受单一样本剧烈影响？
- 相似度距离分布是否集中在某个区间（如大量 Very High 匹配）？

**验证方法**：
1. 对最近 30~60 个交易日连续运行：
   ```bash
   quant-cli research calibration --scope global
   quant-cli research calibration --scope cn
   ```
2. 命令会自动运行 `confirmation`、`recovery`、`analogues` 并聚合统计。
3. 观察：
   - 指标是否具有足够的日内/日间区分度
   - 是否存在异常值（如 Recovery 连续多日 100 或 Confirmation 永远 Moderate）
   - 相似日匹配结果是否稳定（不同日期不会频繁匹配到同一批异常日）
   - 权重、阈值、距离函数是否需要微调
   - 距离分布直方图是否显示匹配过度集中在某个 bucket

**输出**：
- `reports/calibration/research-calibration-{scope}-{start}-{end}.md`：记录四章节观察结果与调整建议。
  - 报告头包含 `Calibration Baseline Version` 与 `Generated At`
  - 第 3 章包含 `Distance Distribution (all target-vs-historical pairs)` 直方图
- 如果行为稳定，才允许进入 V7.3 Consensus。

**Phase B+ 新增规则**：
1. 每次 Calibration 报告必须携带 `Baseline Version`（从 1 开始，方法/结构不兼容变更时递增）。
2. 报告头必须记录 `Generated At` 与 `Window`，使多次运行可比较。
3. 必须输出 `Distance Distribution` 直方图，用于判断阈值是否过松或过紧。
4. 在 Phase B+ 期间，仅允许调整 Calibration 报告的统计维度与元数据；不允许修改距离函数、Normalizer、Matcher 或 Fingerprint 结构。
5. Phase B+ 完成后，Calibration Baseline 进入冻结；V7.3 Consensus 只能基于冻结后的基线运行。

**约束**：
- Calibration 期间可以微调权重、阈值、距离函数，但不能修改 Fingerprint 结构。
- 任何调整必须回写本文件和 ADR-071，作为 Evidence Layer 冻结的一部分。
- Phase B+ 期间暂时不修改距离函数，只增加统计维度，为后续阈值调整提供数据依据。

---

## 5. V7.3：Research Synthesis Layer

### 5.0 V7.3 启动条件

V7.3 Consensus 不允许反向修改 V7.1 / V7.2 的契约。因此启动前必须满足：

1. **Architecture Stable**：Fingerprint Builder、DistanceMetric、Normalizer、SimilarityMatcher、SearchResult、OutcomeProfile 的接口和职责已冻结，并形成 ADR-071。
2. **Compile Stable**：`cargo check` 与相关 crate 测试通过。
3. **Behavior Stable**：V7.2C Research Calibration 已完成，对最近 30~60 个交易日的观察显示：
   - `confirmation` / `recovery` / `analogues` 输出具有区分度，无长期固定等级或异常跳变。
   - Historical Analogues 的匹配结果符合市场直觉，对相似日的前向收益统计稳定。
   - 权重、阈值、距离函数已调整到可接受水平，或明确记录为后续实验项。
4. **Semantic Stable**：LLM / 人类可以稳定解释每个输出的含义，输出与底层 Observation / Evolution 数据一致。
5. **Calibration Baseline Frozen**：V7.2C Phase B+ 已完成，Calibration Report 已携带版本化基线（`Baseline Version`）、生成时间（`Generated At`）和距离分布直方图（`Distance Distribution`），并且至少对 Global 和 CN 两个 scope 各运行过一次，报告已写入 `reports/calibration/`。

> **冻结纪律**：Consensus 只能消费 Observation、Evolution、Evidence 三层输出，不能修改它们的结构或计算逻辑。

### 5.1 Consensus（Research Synthesis）

> **V7.3 已批准的边界**：Consensus 不是策略决策器。它永远只回答 "Research 层怎么看"，不回答 "今天买不买"。

**输入**（Evidence Aggregation，不是规则树）：
- Signal
- Stretch
- Confirmation
- Recovery
- Historical Analogues / Outcome Profile

**输出**：
- `bias`：研究层的综合倾向，使用研究语言（`Constructive` / `Neutral` / `Conflicted` / `Fragile` / `Cautious`）。
- `confidence`：置信度（`Low` / `Medium` / `High`）。
- `supporting_evidence`：支持当前 bias 的证据列表，每项带证据类型与权重。
- `contradicting_evidence`：与当前 bias 矛盾的证据列表，每项带证据类型与权重。
- `summary`：一段面向研究人员的解释文字。

**输出示例**：
```text
Research Consensus
  Bias:                Constructive
  Confidence:          Medium
  Supporting Evidence:
    - Recovery Index improving (weight 0.28)
    - Confirmation Moderate (weight 0.24)
    - Rotation Leadership Broadening (weight 0.11)
  Contradicting Evidence:
    - Stretch Extreme (weight -0.16)
    - Crowding High (weight -0.13)
  Summary:             Recovery and Confirmation support a constructive view, but Stretch and Crowding warn that the advance is narrow and crowded. Confidence is Medium.
```

**输出枚举**：
```rust
enum ConsensusBias {
    Constructive,
    Neutral,
    Conflicted,
    Fragile,
    Cautious,
}
```

**注意**：
- Consensus 不是 Bullish/Bearish 判断。
- Consensus 不输出买卖建议、仓位建议、目标价或止损。
- 新增证据类型时，不应推倒 Consensus 逻辑；应加入 Evidence Aggregation 层并重新跑 Calibration。

**实现位置**：
- 纯计算：`crates/core-domain/src/research/consensus.rs`：实现 `EvidenceAggregator` 和 `ConsensusSummary`。
- 语义契约：`crates/research-context/src/lib.rs` 新增 `ConsensusSummary`（含 `bias`、`confidence`、`supporting_evidence`、`contradicting_evidence`、`summary`）。
- 文档输入：`crates/report-builder/src/lib.rs` 新增 `ConsensusReportInput`。
- CLI 命令：`research consensus --scope global|cn|hk`。

**Evidence Aggregation 原则**：
1. 每项证据贡献一个 `(source, weight)`，weight 是 0~1 的连续值，可用正负表示方向。
2. 聚合函数采用加权求和 + 阈值映射，便于解释和后续调整。
3. 权重可在 Calibration 中调整，但不应引入硬编码 `if-else` 规则树。
4. 权重和阈值变更需要重新跑 Calibration，并在报告中记录 Baseline Version 递增。

### 5.2 V7.3.1 Consensus Stabilization（热修复）

在 V7.3 实现完成后，Oracle Review 指出 Consensus 层的可维护性缺口。V7.3.1 作为热修复，只做四件事：

1. **可配置化 `ConsensusConfig`**：在 `core-domain::research::consensus` 中引入 `ConsensusConfig`，聚合 `EvidenceWeights`、`BiasThresholds`、`ConfidenceThresholds`、`ConflictedThresholds`。`app-service` 与 CLI 使用 `ConsensusConfig::default()` 而非散落魔数。默认值保持 V7.3 行为：
   - 权重：Signal 0.20、Stretch -0.18、Confirmation 0.24、Recovery 0.28、Analogues 0.10。
   - Bias 阈值：≥ 0.15 为 Constructive，≤ -0.15 为 Cautious，否则 Neutral。
   - Confidence 阈值：|aggregate| ≥ 0.4 为 High，≥ 0.15 为 Medium，否则 Low。
   - Conflicted 阈值：supporting 与 contradicting 的绝对差 ≤ 0.10 时标记为 Conflicted。
2. **版本化 `ConsensusSummary`**：为 `ConsensusSummary` 增加 `version` 字段（从 1 开始），报告头渲染 `Consensus version: {version}`，用于长期漂移检测。
3. **Calibration Baseline Version 常量**：在 `core-domain::research::calibration` 中定义 `CURRENT_CALIBRATION_BASELINE_VERSION`，`app-service` 的 `run_research_calibration` 引用该常量而非硬编码 `1`。该常量仅在 Evidence 语义变化时递增，实现优化不触发递增。
4. **移除死分支**：删除 `classify_bias` 中不可达的 `score.abs() < 0.01` 分支，使 bias 分类完全由阈值结构决定。
5. **Consensus 垂直切片集成测试**：在 `crates/app-service/src/lib.rs` 增加 `tests::consensus_report_vertical_slice_renders_markdown`，验证从 `EvidenceInput` → `consensus()` → `ResearchContext` → `ConsensusReportInput` → Markdown 渲染的完整链路，并断言输出包含研究语言字段、不含买卖建议。

**验证状态**：`cargo check` 与 `cargo test -p core-domain -p app-service -p quant-cli -p report-builder -p research-context -p reporting` 通过；Global / CN Consensus 报告已用版本 1 重新生成。

---

## 6. Rotation 升级（非独立模块）

不新建 `Rotation Flow` 一级模块。升级现有 `ResearchContext::RotationSummary`：

```rust
struct RotationSummary {
    pub top: Vec<RotationItem>,              // 已有
    pub bottom: Vec<RotationItem>,           // 已有
    pub rotation_state: String,              // 已有：broad / concentrated
    pub leadership_stability: f64,           // 已有
    pub leadership_transition: String,       // 新增：leader 是否切换 / 切换方向
    pub rotation_acceleration: f64,          // 新增：轮动速度变化
    pub theme_dispersion: f64,             // 新增：主题分散度
}
```

**计算位置**：`crates/core-domain/src/research/rotation.rs`（当前占位文件）。

**约束**：
- 所有新增字段必须是 additive，不影响现有消费者
- 旧数据反序列化通过 `#[serde(default)]` 兼容

---

## 7. 不做的事项

| 方向 | 原因 |
|------|------|
| Technology Cycle | 属于 Industry Research，需要产业链数据（HBM/订单/CapEx） |
| Capital Flow | 当前无资金流数据，只有 Rotation 相对强弱 |
| Industry Cycle | 越界到 Market External |
| Rotation Flow 独立模块 | 会导致 Semantic Duplication，应升级 RotationSummary |
| Transition 独立 Summary | 应作为 Consensus 解释，避免与 Recovery/Confirmation 冲突 |

---

## 8. CLI 命名约定

统一使用 `research` 二级命令结构：

```bash
quant-cli research confirmation    # V7.1
quant-cli research recovery      # V7.1
quant-cli research analogues     # V7.2
quant-cli research consensus     # V7.3
```

旧命令保留：
```bash
quant-cli research srd
quant-cli research stretch
quant-cli research analytics
quant-cli research review
```

---

## 9. 实施顺序

### Phase 1：V7.1（Market Evolution）

1. 新建 `core-domain::research::confirmation`
2. 新建 `core-domain::research::recovery`
3. 升级 `core-domain::research::rotation`（填充 Leadership Evolution）
4. 扩展 `ResearchContext`：新增 `ConfirmationSummary`、`RecoverySummary`
5. 扩展 `report-builder`：新增 `ConfirmationReportInput`、`RecoveryReportInput`
6. 扩展 `reporting::SectionKind`：新增 `Confirmation`、`Recovery`
7. 新增 CLI 命令：`research confirmation`、`research recovery`

### Phase 2：V7.2A（Market Fingerprint Foundation）

1. 新建 `crates/market-fingerprint-engine/`
2. 定义 `MarketFingerprint`、`ObservationVector`、`EvolutionVector`
3. 实现 `MarketFingerprintBuilder`（从 `ResearchContext` 构建）
4. 添加契约测试，冻结 Fingerprint 结构

### Phase 3：V7.2B（Evidence Retrieval Engine）

1. 实现 `Normalizer` / `FeatureVector`
2. 定义 `DistanceMetric` trait 与 `CosineDistance` 实现
3. 实现 `SimilarityMatcher` / `find_similar()`
4. 实现 `OutcomeProfiler` / `OutcomeProfile`
5. 扩展 `report-builder`：新增 `AnalogueReportInput`
6. 新增 CLI 命令：`research analogues`
7. 提交 ADR-071，冻结 Evidence Layer 架构规则

### Phase 4：V7.2C（Research Calibration）

1. 对最近 30~60 个交易日连续运行 `research confirmation` / `research recovery` / `research analogues`
2. 记录输出等级、关键驱动、匹配日期、前向收益统计
3. 检查区分度、异常值、稳定性
4. 形成 `reports/calibration/research-calibration-{scope}-{start}-{end}.md`
5. 确认是否满足 V7.3 启动条件

### Phase 5：V7.3（Research Synthesis）

1. 新建 `core-domain::research::consensus`
2. 扩展 `ResearchContext`：新增 `ConsensusSummary`（内含 Transition）
3. 扩展 `report-builder`：新增 `ConsensusReportInput`
4. 扩展 `reporting::SectionKind`：新增 `Consensus`
5. 新增 CLI 命令：`research consensus`
6. **V7.3.1 稳定化**：
   - 引入 `ConsensusConfig`（EvidenceWeights + BiasThresholds + ConfidenceThresholds + ConflictedThresholds）
   - 为 `ConsensusSummary` 增加 `version` 字段
   - 将 `Calibration Baseline Version` 提取为 `core-domain::research::calibration` 常量
   - 移除 `classify_bias` 死分支
   - 新增 `app-service` 集成测试覆盖 Consensus 垂直切片

---

## 10. 前置依赖与约束

### 10.1 必须遵守的冻结纪律

| 层级 | 状态 | 说明 |
|------|------|------|
| State Layer | 冻结 | 策略状态逻辑不可修改 |
| Signal Engine | 冻结 | 信号权重、阈值不可修改 |
| Execution Layer | 冻结 | 执行过滤逻辑不可修改 |
| V6 Reporting Platform | 冻结 | `reporting` / `report-builder` 平台本身不可重构，但可在平台上新增 SectionKind / ReportInput |
| Production Surface | 冻结 | DashboardSnapshot、export-report 展示逻辑不可修改 |
| V7.1 Market Evolution | 冻结 | `ConfirmationSummary`、`RecoverySummary`、升级后的 `RotationSummary` 结构不再变化 |
| V7.2 Evidence Layer | 冻结 | Fingerprint、Matcher、SearchResult、DistanceMetric、OutcomeProfile、Calibration 接口已锁定 |
| V7.3 Research Synthesis | 冻结 | `Consensus`、`EvidenceAggregation`、`ConsensusConfig`、`ConsensusSummary` 不再变化 |
| Research Platform 1.0 | 冻结 | Observation + Evolution + Evidence + Consensus 整体冻结；新增内容属于 Research Content Evolution，不修改 Semantic Architecture |

### 10.2 架构约束

- V7 新增语义必须证明是**跨消费者共享的独立市场语义**，否则只能作为 `ReportInput` 的文档专属字段。
- **ADR-077 之后**：新增 Observation、指标、因子、内容属于 Research Content Evolution，必须通过现有冻结层消费；修改 Semantic Architecture 需要新的 ADR 和显式解冻。
- `Transition` 不进入 `ResearchContext`，只存在于 `ConsensusSummary`。
- `Historical Analogues` 不对外暴露原始相似度百分比。
- `Rotation` 不独立为一级模块，只升级 `RotationSummary`。

### 10.3 环境约束

- Eastmoney 不可达，全部标的走 Tencent fallback
- FRED 宏观因子依赖外部 API
- 静态 JSON 日历覆盖 2024-2027
- Shadow Production 观察期继续运行

---

## 11. 与 V6 的边界

V7 **明确不做**：
- ❌ 修改任何 Quant Engine 决策逻辑
- ❌ 基于 Confirmation / Recovery / Analogues 自动调整仓位
- ❌ 新增 Technology Cycle / Industry Cycle 等 Market External 研究域
- ❌ 将 Research 输出接入 Strategy State 或 Execution
- ❌ 把 Transition 作为独立语义放入 ResearchContext

V7 **在 V6 基础上新增**：
- ✅ Confirmation（Market Evolution）
- ✅ Recovery（Market Evolution）
- ✅ Market Fingerprint Engine（Historical Evidence）
- ✅ Historical Analogues（Historical Evidence）
- ✅ Outcome Profile（Historical Evidence）
- ✅ Consensus（Research Synthesis，含 Transition）
- ✅ RotationSummary 升级（Leadership Evolution）

---

## 12. ADR

- **ADR-070**：Market Evolution Semantic Layer
- 关联：ADR-068（Research Context + Reporting Layer v1）、ADR-069（Reporting Layer Architecture Invariants）
- 记录内容：
  - V7 从 "功能列表" 收敛到 "语义能力" 的决策
  - 四层架构定义
  - Transition 属于 Consensus 的判断
  - Rotation 不独立、Historical Analogues 不暴露百分比等约束
  - 未来新增语义必须是跨消费者共享的独立市场语义

---

## 13. 相关文档

- `docs/architecture-invariants.md` — 不可违反的分层规则（ADR-069）
- `docs/v6/adr-068-research-context-reporting-layer.md` — V6 Reporting Layer 设计
- `memory/context.md` — 当前运行时状态
- `memory/decisions.md` — 架构决策记录
- `设计规划-v6.md` — V6 设计规划