# 2026-05-20 Oracle 数据质量复核报告修复

## 变更内容

基于 Oracle 数据质量复核报告（`数据质量复核报告-Oracle.md`），对系统进行了 6 项修复（P0-P5），涵盖运营修复和代码修复：

### 代码变更（4 个文件）

| 文件 | 行 | 变更内容 | 对应问题 |
|------|-----|----------|----------|
| `crates/data-ingestion/src/lib.rs` | 287 | `turnover: None` → `turnover: row.get(6).and_then(\|v\| v.parse().ok())` | P2 Turnover 缺失 |
| `crates/app-service/src/lib.rs` | 857-861 | 新增 `REGISTRATION_BOARD_INDICES` 常量，注册制板块 Index 阈值 12% → 22% | P4 跳变误报 |
| `crates/market-store/src/lib.rs` | 1308 | 移除 `WHERE market = 'GLOBAL'` 过滤，全 scope regime 加载 | P5 GLOBAL-only bug（Oracle 未覆盖） |
| `PROJECT_STRUCTURE.md` | 新建 | 项目结构说明书（含 Mermaid 依赖图） | 文档建设 |
| `memory/structure.md` | 更新 | 同步最新目录结构与 workspace 成员 | Memory 维护 |
| `memory/decisions.md` | 追加 | P0/P2/P4/P5 决策记录 | Memory 维护 |
| `memory/context.md` | 覆盖 | 更新当前阶段与目标 | Memory 维护 |

### 运营修复（CLI 命令执行）

- `compute-macro --from 2020-01-01 --to 2026-05-19`：生成 7,149 macro 行 + 各 2,442 regime/environment/strategy_state 行
- `compute-signals`：重跑后 `regime_missing` 17,195 → 152，`data_starved` 52.6% → 2.9%

### 验证结果

- `cargo check`：全部通过，0 errors
- `pipeline-dates`：9/9 阶段 is_latest=true，alerts=[]
- `dashboard-snapshot`：report_date=2026-05-19，信号质量正常
- `export-report`：成功产出 `daily-report-2026-05-19.md`
- `check-data-health`：P4 生效（注册制板块 0 suspicious jumps）

## 原因

Oracle 复核报告揭示了 5 个数据质量问题（P0-P4）。修复过程中额外发现 P5——`fetch_market_regimes` 的 GLOBAL-only 过滤——这是 Phase 2 per-scope regime 落地后遗留的关键 bug，导致 CN/HK scoped 信号自上线以来一直使用 fallback 值。

## 影响

- **信号质量重塑**：52.6% signals 从"盲飞"（fallback 50.0）变为基于真实 regime 数据计算。
- **数据健康噪音消除**：注册制板块真实极端行情不再误报。
- **Turnover 解析就绪**：新拉取数据将包含 turnover，修复需 `ingest-daily` 回填存量。
- **Memory 体系更新**：decisions/context/structure 全部同步至当前状态。
- **残余项**：152 个边缘 regime_missing（<0.5%），840 个 rotation_missing 待下钻，HSAHP 处置待决策。
