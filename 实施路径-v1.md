# Rust Quant Analysis System V1 实施路径

## 1. 已确认的产品与技术方向

- 产品形态：本地桌面研究工具
- 技术栈：Rust + Tauri + ClickHouse（Docker）
- 前端：Tauri Web UI（建议 Vue3 + ECharts + Lightweight Charts）
- 研究对象：仅 Index + ETF
- 实现方式：从零开始，在 `F:\ai_area\rust-quant-analysis-system` 下重新建设，不保留 Python 主实现
- Python 旧系统用途：仅作为参考样本，学习优点，不直接迁移为双语言架构

## 2. V1 范围定义

### 2.1 V1 必做

1. 指数 / ETF 基础元数据管理
2. 日线行情采集、清洗、落库
3. 技术指标计算
4. 市场环境判断
5. 行业 / ETF 轮动排名
6. 四类策略偏好全量评分
7. 宏观因子纳入 V1
8. 最终信号生成
9. 基础回测
10. Tauri 桌面端分析界面
11. 每日报告导出

### 2.2 V2 再做

- 个股体系
- 自动交易
- 多用户 Web API
- 云端回测 / 任务调度平台化

## 3. 关键架构决策

## 3.1 总体架构

```text
Tauri Frontend
    ↓ invoke
Rust App Service
    ├─ Data Ingestion
    ├─ Indicator Engine
    ├─ Macro Engine
    ├─ Rotation Engine
    ├─ Strategy Preference Engine
    ├─ Signal Engine
    ├─ Backtest Engine
    └─ Report Engine
         ↓
ClickHouse (Docker)
```

## 3.2 为什么不建议“全量 ClickHouse”

用户提到如果全量 ClickHouse 更合理也可以接受，但从工程角度看，V1 更合理的方案仍然是：

- **ClickHouse**：负责分析型、时序型、批量计算型数据
- **SQLite**：负责轻量事务型本地状态

原因：

1. ClickHouse 擅长 append-heavy 分析查询，不擅长桌面应用的配置与细粒度状态管理。
2. 用户偏好、任务执行状态、UI 布局、本地缓存索引等数据天然更适合 SQLite。
3. 如果把所有内容都堆进 ClickHouse，会让 Tauri 本地应用的恢复、迁移、升级变复杂。

### 3.2.1 建议的数据职责划分

#### ClickHouse 中保存

- instrument 基础信息快照
- daily_bar
- indicator_snapshot
- macro_snapshot
- market_regime
- rotation_rank
- strategy_preference
- signal_snapshot
- backtest_run
- backtest_equity_curve
- report_snapshot

#### SQLite 中保存

- app_config
- data_source_credentials
- job_run_log
- ui_preferences
- local_watchlist
- cache_manifest

> 结论：V1 推荐 **ClickHouse + SQLite**。如果后续你强制要求全量 ClickHouse，可以做，但我不建议作为初版方案。

## 4. V1 研究范围收敛

### 4.1 标的范围

- A 股核心指数
- H 股核心指数
- A/H 指数 ETF
- 行业 ETF
- 主题 ETF（可选，放在 instrument 分类层支持）

### 4.2 统一抽象

V1 所有分析对象统一建模为 `Instrument`：

- `instrument_type = INDEX | ETF`
- `market = CN | HK`
- `category = BROAD | INDUSTRY | THEME | OVERSEAS_LINK`

这比前一版规划里的“个股 / ETF / 指数混用模型”更稳。

## 5. 模块设计

## 5.1 data-ingestion

职责：

- 拉取指数/ETF日线
- 标准化字段
- 数据清洗与幂等写入
- 维护连续序列

输入源建议（按当前实现与可靠性复核后修订）：

- CN：**Eastmoney 主源**，**Tencent 兜底**；Tushare 作为后续可选增强源（需 token / 积分）
- HK：**Eastmoney / Tencent 低成本组合优先**；Yahoo Finance 仅作为可选备选，不作为默认主源

说明：

- 当前运行环境中，Yahoo Finance 的港股历史接口已实测出现 `403`，因此不宜作为默认可靠源
- 现有项目已经基于 Eastmoney + Tencent 跑通完整 V1 链路，继续沿用能降低实现风险

输出：

- `daily_bar`
- `instrument_snapshot`

## 5.2 indicator-engine

职责：

- 计算 MA / EMA / MACD / RSI / ATR / VOL_MA
- 为策略评分提供统一指标快照

输出：

- `indicator_snapshot`

建议字段：

- ma10, ma20, ma30, ma60, ma120
- ema12, ema26
- macd, macd_signal, macd_hist
- rsi14
- atr14
- vol_ma20, vol_ma60

## 5.3 macro-engine

职责：

- 将宏观因子正式纳入 V1
- 生成市场层面的风险与顺风/逆风判断

V1 推荐宏观因子：

- 利率 / 无风险收益率代理
- 美元指数代理
- 波动率代理
- 流动性代理
- 风险偏好代理

输出：

- `macro_snapshot`
- `market_regime`

注意：

V1 的宏观因子不做过于复杂的预测模型，只做 **规则化 regime 识别**。

## 5.4 rotation-engine

职责：

- 计算 20 / 60 / 120 日相对强弱
- 给 ETF / 行业做轮动排序

输出：

- `rotation_rank`

建议核心字段：

- rs_20
- rs_60
- rs_120
- momentum_score
- rank

## 5.5 strategy-preference-engine

这是 V1 核心模块之一。

四类策略全部纳入 V1：

1. `VALUE_LEFT`
2. `TREND_PULLBACK`
3. `TREND_BREAKOUT`
4. `MOMENTUM_RIGHT`

输出：

- `strategy_preference`

建议字段：

- date
- symbol
- value_left_score
- trend_pullback_score
- trend_breakout_score
- momentum_right_score
- best_strategy
- confidence
- alignment

设计原则：

- 每个策略单独实现评分器 trait
- 统一输入 `AnalysisContext`
- 统一输出 `StrategyPreferenceScore`

## 5.6 signal-engine

职责：

- 汇总 macro / market / rotation / strategy preference
- 生成最终操作信号

建议信号模型：

```text
final_score =
  best_strategy_score * 0.45
  + alignment_score * 0.15
  + market_regime_score * 0.20
  + rotation_score * 0.20
```

输出：

- `signal_snapshot`

建议信号枚举：

- STRONG_BUY
- BUY
- WATCH
- HOLD
- REDUCE
- SELL

## 5.7 backtest-engine

职责：

- 基于 `signal_snapshot` 跑简化事件驱动回测
- 支持单策略、组合策略、策略偏好组合

V1 支持：

- 日频回测
- 固定手续费 / 滑点模型
- 仓位上限
- 最大持仓数

输出：

- `backtest_run`
- `backtest_trade`
- `backtest_equity_curve`

## 5.8 report-engine

职责：

- 导出日报
- 生成市场状态摘要
- 输出 ETF 机会榜
- 输出策略解释摘要

输出：

- CSV / JSON / HTML 报告

## 5.9 tauri-app

职责：

- 展示图表、表格、策略解释
- 触发任务执行
- 展示任务状态和最近结果

重要约束：

- 前端不能直接操作 Docker
- 前端不能直接拼 SQL
- 前端只能通过 Tauri command 调 Rust service

## 6. 推荐仓库结构

```text
rust-quant-analysis-system/
├─ docs/
│  ├─ architecture/
│  ├─ schema/
│  └─ decisions/
├─ infra/
│  └─ docker/
│     └─ docker-compose.yml
├─ apps/
│  ├─ cli/
│  └─ desktop/
│     ├─ src/
│     └─ src-tauri/
├─ crates/
│  ├─ core-domain/
│  ├─ app-service/
│  ├─ data-ingestion/
│  ├─ market-store/
│  ├─ indicator-engine/
│  ├─ macro-engine/
│  ├─ rotation-engine/
│  ├─ strategy-engine/
│  ├─ signal-engine/
│  ├─ backtest-engine/
│  ├─ report-engine/
│  └─ task-runner/
└─ sql/
   ├─ clickhouse/
   └─ sqlite/
```

## 7. 数据库表设计（V1 最小可用版）

## 7.1 ClickHouse

### 基础表

- `instrument`
- `daily_bar`

### 计算表

- `indicator_snapshot`
- `macro_snapshot`
- `market_regime`
- `rotation_rank`
- `strategy_preference`
- `signal_snapshot`

### 回测表

- `backtest_run`
- `backtest_trade`
- `backtest_equity_curve`

### 报告表

- `report_snapshot`

## 7.2 SQLite

- `app_config`
- `job_run_log`
- `ui_preferences`
- `credential_store`

## 8. ClickHouse 设计建议

### 8.1 引擎

- 主表使用 `MergeTree`

### 8.2 分区

- 推荐 `PARTITION BY toYYYYMM(date)`

### 8.3 排序键

- 推荐 `ORDER BY (symbol, date)` 或者按主要查询模式调整

### 8.4 写入策略

- 严禁大量单行插入
- 使用批量写入
- 需要时使用 async insert

### 8.5 Docker 部署建议

- 只绑定 localhost 端口
- 挂载持久化 volume
- 配置 healthcheck
- Rust 启动时检查 ClickHouse ready 状态

## 9. 分阶段实施路径

## Phase 0：脚手架与基础设施

交付物：

- Cargo workspace
- Tauri app skeleton
- Docker Compose for ClickHouse
- SQLite init
- 配置文件系统
- 日志系统

验收标准：

- CLI 能启动
- Desktop 能启动
- ClickHouse 容器能健康运行

## Phase 1：数据层

交付物：

- instrument 导入
- daily_bar 入库
- 幂等更新流程
- 数据源适配器第一版

验收标准：

- 能导入一组指数 + ETF 的近 5~10 年日线
- 重复执行不会产生脏重复数据

## Phase 2：指标层

交付物：

- MA / EMA / MACD / RSI / ATR / VOL_MA
- indicator_snapshot

验收标准：

- 任意 symbol 可查询最近 N 日指标快照

## Phase 3：宏观与市场环境

交付物：

- macro_snapshot
- market_regime
- 市场风险分层

验收标准：

- 系统能给出每日 market regime

## Phase 4：轮动与强弱排序

交付物：

- rotation_rank
- ETF 强弱榜

验收标准：

- 能输出 Top N 行业 / ETF 强度排行

## Phase 5：四类策略偏好全量评分

交付物：

- VALUE_LEFT scorer
- TREND_PULLBACK scorer
- TREND_BREAKOUT scorer
- MOMENTUM_RIGHT scorer
- strategy_preference 表

验收标准：

- 每个 ETF 每日都能给出四类策略评分与 best_strategy

## Phase 6：最终信号层

交付物：

- signal_snapshot
- 最终买卖/观察信号
- 策略解释摘要

验收标准：

- 系统能解释“为什么给出这个信号”

## Phase 7：回测层

交付物：

- 日频回测
- 收益曲线
- 交易记录
- 关键绩效指标

验收标准：

- 可以比较不同策略和组合规则表现

## Phase 8：桌面 UI 与报告

交付物：

- Dashboard
- ETF 分析页
- 回测页
- 设置页
- 日报导出

验收标准：

- 用户能在桌面端完成核心研究闭环

## 10. V1 页面建议

### Dashboard

- 市场环境
- 风险等级
- Top ETF 机会榜
- 今日强弱轮动

### ETF 分析页

- K线
- 指标
- 四策略评分
- 宏观顺逆风
- 历史信号

### 回测页

- 策略选择
- 参数配置
- 收益曲线
- 回撤分析
- Trade 明细

### 设置页

- 数据源配置
- 本地数据库状态
- ClickHouse 状态
- 更新任务控制

## 11. 与 Python 旧系统对齐时应学习的优点

旧系统值得学习的不是语言，而是分层思想：

1. 数据、信号、解释层分离
2. summary / decision / report 是逐层加工关系
3. 不只给 BUY/SELL，还给“为什么”
4. Dashboard 面向使用者，而不只是技术输出

因此 Rust 新系统也要保留：

- 原始指标层
- 策略偏好层
- 最终信号层
- 报告解释层

## 12. 当前建议的最终结论

V1 的最佳形态是：

**Rust Core + Tauri Desktop + ClickHouse Analytics + SQLite Local State**

并且范围上严格收敛为：

**指数 / ETF + 宏观因子 + 四类策略偏好 + 最终信号 + 基础回测 + 报告**

这版方案比原始规划更一致，也更适合直接进入工程实施。
