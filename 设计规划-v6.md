# V6 设计规划：Research Layer v1 — 观测增强与证据积累

> 本文件用于持续记录 V6 阶段的设计规划、技术决策与实施方向。
> 创建日期：2026-06-26
> 前置阶段：V5（Execution Layer + Explainability Layer）已落地，Shadow Production 观察期
> 当前状态：Phase A 已完成（Bug 修复 + Research Layer 工具建设 + 使用手册）

---

## 1. 目标概述

V6 的核心目标**不是**扩展量化决策能力，而是：

> **构建 Research Layer — 让"值得怀疑的现象"变成可观测、可统计、可解释的研究信号，而不是急于把它们变成新的交易规则。**

完成从：
```
"发现背离 → 建议修改系统"
```
到：
```
"发现背离 → 收集证据 → 统计验证 → ADR 决策"
```

的范式转换。

这是量化系统从**经验优化**走向**证据驱动优化**的关键一步。

---

## 2. 核心架构边界

### 2.1 三层架构（V6 固化）

```
Quant Engine (Frozen)
├── State Layer         — 策略状态
├── Signal Engine       — 信号生成
├── Execution Layer     — 执行过滤
└── Backtest Engine     — 回测

Research Layer (Expandable)
├── Diagnostics         — 解释现有决策
├── Observation         — 提出假设，收集证据（SRD、Market Stretch）
├── Narratives          — LLM 条件推演（Possible Scenarios）
└── Explainability      — 归因拆解（symbol-diagnostics, symbol-scoreboard）

ADR Process (Gate)
└── 决定是否依据长期证据修改 Quant Engine
```

### 2.2 Research Layer 治理原则

> **Research Layer 可以发现新的现象，但不能创造新的事实。**

| 允许 | 禁止 |
|------|------|
| 解释现有决策的构成 | 生成新的评分、排名或决策信号 |
| 统计背离现象的持续天数与分位 | 基于统计结果自动修改仓位或信号 |
| 输出条件推演（"如果 X 发生，则可能 Y"） | 输出方向预测（"未来将上涨"） |
| 描述市场拉伸程度与证据 | 基于拉伸程度给出操作建议 |

这些原则将写入 ADR，作为所有 Research Layer PR 的 Review 基线。

---

## 3. Phase A：立即执行

执行顺序：**Bug 优先 → 可读性优化 → Research 工具 → LLM 增强**

### 3.1 A1：修复 Data Health 持续不可用（P0）

**现状**：连续五天 Trust Summary 中 `Macro Status: unknown`、`Data Health: N/A`、`Latest-Day Complete: no`。

**目标**：排查并修复 FRED 数据源 → ClickHouse 查询 → Trust Summary 组装链路中的断点，恢复 Data Health 可用性。

**增强要求**：修复后，Data Health 输出需包含缺失标的具体清单与原因，而非仅 `0/26`。

```
Coverage
  26 expected
  24 available
  2 missing: 512480, 159995
  Reason: Waiting latest bar
```

### 3.2 A2：修复 Coverage 统计异常

**现状**：连续五天 `Scoped Latest-Day Coverage: 0/26`。

**目标**：排查 coverage 统计口径（是真正缺数还是计算逻辑错误），修复后输出预期/可用/缺失明细。

### 3.3 A3：Trust Summary 信息分层重构

**现状**：25+ 行平铺输出，关键信息（State、Regime、Divergence）淹没在大量 `N/A` 和 `no` 中。

**目标**：重组为固定三层结构，格式永久稳定。

```
[CRITICAL]
  State           — 策略状态与推荐仓位
  Regime           — 市场状态与 Lag
  Position         — 当前仓位
  Signal-Regime Divergence  — 背离追踪

[WARNING]
  Coverage         — 数据覆盖情况
  Macro Status     — 宏观因子状态
  Data Health      — 数据健康摘要

[INFO]
  Backtest         — 回测匹配情况
  Rotation         — 轮动概况
  Notes            — 补充说明
```

**约束**：仅调整输出格式，不修改内容生成逻辑。

### 3.4 A4：SRD 统计工具（`research srd`）

**目标**：新建 CLI 工具，纯统计输出 Signal-Regime Divergence 的持续情况。

**输出格式**：
```
SRD Statistics
  Duration:              5 days
  StrongBuy count:       5
  Buy count:             0
  Average Signal:        82.3
  Breadth trend:         Improving
  Rotation pattern:      Technology Dominant
  Historical percentile: 97%
```

**约束**：
- 不输出评分、不输出结论、不输出建议
- 不进入任何决策链路
- 属于 Observation 子层

### 3.5 A5：Market Stretch 工具（`research stretch`）

**目标**：新建 CLI 工具，描述市场的拉伸/拥挤程度。

**命名**：使用 `Market Stretch` 而非 `Market Temperature` 或 `BubbleScore`——强调"拉伸程度"这一客观描述，避免形成隐性评分体系。

**输出格式**：
```
Market Stretch
  Overall:               Elevated

  Crowding:              Elevated
    Evidence:            Top5 Rotation = 68%
                         Historical Percentile = 95%

  Breadth:               Normal
    Evidence:            Breadth = 61%
                         SMA5 = 63%

  Momentum:              Extreme
    Evidence:            RS120 Max = 89.7
                         RS120 Top5 Avg = 81.2

  Leverage:              Normal
    Evidence:            Margin Balance Trend = Stable
```

**等级系统**：`Normal` / `Elevated` / `Extreme`（三级，仅描述不评分）。

**约束**：
- 每个维度必须附证据，不接受无解释的星级或分数
- 不输出操作建议
- 不进入任何决策链路

### 3.6 A6：LLM 条件推演增强

**目标**：在现有 `market_story` action 的 Prompt 末尾增加条件推演段落，让 LLM 从"解释过去"扩展到"做有条件的推演"。

**新增段落格式**（作为 `market_story` 输出的末段，不新增 action）：
```
## Possible Scenarios

### Base Case
成立条件: {2-3 个可观测条件}
失效条件: {什么观测会推翻此场景}
观察窗口: {短期/中期}
市场含义: {如果成立，意味着什么}

### Alternative Case
成立条件: ...
失效条件: ...
观察窗口: ...
市场含义: ...

### Invalidation Case
触发条件: {什么信号出现意味着当前框架需要重新评估}
观察窗口: ...
```

**约束**：
- 每条场景必须绑定可观测的成立条件和失效条件
- 禁止输出纯方向预测（如"未来将上涨"）
- 属于 Narratives 子层，标注为 Research，非 Prediction

---

## 4. Research Surface 使用手册

### 4.1 定位：不是交易工具，而是"市场发生了什么"

```
Quant Engine (Frozen)
├── Signal / State / Execution / Risk / Backtest
            │
            │ 不允许修改
            ▼
   Research Surface
├── research symbol
├── research scoreboard
├── research rotation
├── research srd
├── research stretch
└── LLM market_story
            │
            ▼
     人类研究员进行判断
```

Research Surface 的核心职责是：
- **把过去只能凭经验感受到的市场状态，变成可长期积累、回测、复盘和讨论的客观统计证据**
- **只观察、只解释、只记录，不干预任何量化决策链路**

### 4.2 如何阅读 `research srd`

`research srd` 回答的问题是：**最近量化系统是不是一直在"打架"？**

即：Signal 很强，但 Regime/State 很保守，两者持续不一致。

| 字段 | 含义 | 如何读 |
|------|------|--------|
| `Duration` | 连续 SRD 交易日数 | `0d` = 今天无背离；`6d` = 已连续 6 个交易日 Signal 与 State 不一致 |
| `StrongBuy count` | 当日 StrongBuy 标的数量 | 反映 Signal 强度 |
| `Buy count` | 当日普通 Buy 标的数量 | 辅助判断 |
| `Average Signal` | 当日整体 Signal 强度 | 40 = 偏弱；80 = 偏强；后续建议改用 Median |
| `Breadth trend` | Breadth 趋势 | Improving / Stable / Weakening |
| `Rotation pattern` | 资金集中方向 | Technology Dominant / Defensive Rotation / Balanced |
| `Historical percentile` | 历史分位 | 97% = 过去几年只有 3% 的日子比现在更严重 |

### 4.3 如何阅读 `research stretch`

`research stretch` 回答的问题是：**市场是不是已经"拉伸"得太厉害了？**

注意：不是"泡沫"、不是"顶部"、不是"该卖了"，而是客观地描述市场拉伸程度。

| 维度 | 指标 | 含义 |
|------|------|------|
| `Crowding` | Top5 Rotation 占比 | 资金是否高度集中。例如 91% = 非常拥挤 |
| `Breadth` | Breadth % + SMA5 | 上涨家数比例及 5 日趋势 |
| `Momentum` | RS120 Max + Top5 Avg | 动量速度是否处于极端 |
| `Leverage` | Margin Balance Trend | 融资余额趋势（当前数据源待接入） |
| `Overall` | 综合等级 | Normal / Elevated / Extreme，附带 Evidence（如 2/4 dimensions elevated） |

### 4.4 建议的每日工作流

收盘后依次运行：

```bash
# 第一步：检查系统内部是否持续打架
cargo run -p quant-cli -- research srd --scope global

# 第二步：检查市场拉伸程度
cargo run -p quant-cli -- research stretch --scope global

# 第三步：让 LLM 基于上述观测撰写日报
cargo run -p quant-cli -- analyze-with-llm --scope global --action market_story
```

LLM 的 `market_story` 已增加 `Possible Scenarios` 段落，会自动结合 SRD 和 Stretch 输出进行条件推演。

### 4.5 为什么需要 Evidence？

以前只有：
```
Overall: Extreme
```

现在：
```
Overall: Extreme
Evidence: 2/4 dimensions elevated (2 Extreme, 0 Elevated)
```

这样人类研究员能立即知道：
- 这个结论不是黑盒规则
- 哪两个维度推高了整体等级
- 是否存在误判可能

### 4.6 Phase B 的 Rolling History 价值

90 天后真正有价值的不是某一天 Extreme，而是历史分布：

```bash
# 未来支持
cargo run -p quant-cli -- research srd --history --days 90
cargo run -p quant-cli -- research stretch --history --days 90
```

输出示例：
```
SRD (last 90 days)
  Longest streak: 6d
  Average streak: 1.8d
  95th percentile: 5d

Market Stretch (last 90 days)
  Extreme: 7
  Elevated: 11
  Normal: 72
```

这些统计将为 Phase C 的 ADR 提供量化背景证据。

---

## 5. Phase B：90 天证据收集

### 5.1 目标

- 每日运行 `research srd` 和 `research stretch`
- 季度产出统计报告，回答以下核心问题：
  - SRD 持续 ≥5 天时，后续 T+20 收益分布如何？
  - Market Stretch = Extreme 是否确实与市场拐点相关？
  - False Positive / False Negative 比例？
  - 当前 Regime 在 SRD 期间的预警胜率？

### 5.2 硬性约束

- SRD 和 Market Stretch 的统计结果**不进入** Strategy State、Signal、Execution、Risk 任何决策链路
- 它们是纯粹的数据收集工具，目的是为未来 ADR 提供证据
- 不输出任何形式的操作建议

### 5.3 Rolling History 统计（Phase B 扩展）

在 90 天证据收集后期，增加自动化的滚动历史统计，减少人工翻日志的成本：

- `research srd --history`：输出过去 N 天内 SRD 的分布统计
  - 最长连续 SRD 天数
  - 平均 SRD 持续天数
  - SRD 出现频率

- `research stretch --history`：输出过去 N 天内 Market Stretch 等级分布
  - Extreme 出现次数
  - Elevated 出现次数
  - Normal 出现次数

这些历史统计用于：
- 判断当前 SRD 或 Stretch 处于历史什么位置
- 为 Phase C 的 ADR 提供可量化的背景证据
- 生成季度 Research Quarterly Review

### 5.5 Analytics Layer — 条件历史分布（Phase B 核心增强）

#### 目标

回答 GPT 提出的核心问题：

> "历史上这种状态之后通常发生什么？"

例如：
- `SRD(StrongBuy≥5, Breadth=Improving, TechDominant)` 历史上出现 N 次，后续 T+20 收益中位数、最大回撤、RISK_ON 转化率分别是多少？
- `Stretch(Overall=Extreme, Crowding=Extreme, Momentum=Extreme, Breadth=Normal)` 历史上出现 N 次，后续 5d/10d/20d 平均收益与最大回撤分别是多少？

#### 设计原则

- **只读研究层**：Analytics Layer 只能统计历史，不能修改 Signal / State / Execution。
- **复用已有基础设施**：`regime-audit/src/external_validation.rs` 已具备按 regime 标签计算未来收益分布的能力，应抽取为共享 utility。
- **不创建新交易 score**：输出的是历史统计（median return、win rate、max drawdown），不是投资建议。
- **渐进式**：MVP 先做预定义条件；高级阶段再支持自定义条件 DSL。

#### 架构 proposal

```text
apps/cli/src/commands/research.rs        # 新增 research-analytics 命令
    ↓
crates/research-analytics/src/lib.rs      # 纯计算层（新增 crate）
    ├── condition.rs                       # SrdCondition / StretchCondition
    ├── forward_return.rs                  # 未来收益/回撤/波动率/胜率统计
    └── transition.rs                      # Regime / State 转移概率
    ↓
crates/regime-audit/src/external_validation.rs  # 抽取共享 forward-return 工具函数
    ↓
crates/market-store/src/                  # 数据读取（signals / rotation / environment / bars / regime / state）
```

#### 新增 crate：crates/research-analytics

职责：
- 接收一个 `Condition` 和 `horizon_days`
- 遍历历史日期，匹配条件
- 对每个匹配日，读取 anchor symbol（000300 / HSCEI）未来 N 日收盘价
- 聚合输出统计量

核心 DTO：

```rust
pub struct ConditionalForwardReturnStat {
    pub condition_name: String,
    pub horizon_days: usize,
    pub occurrence_count: usize,
    pub forward_return_median: f64,
    pub forward_return_mean: f64,
    pub forward_return_p25: f64,
    pub forward_return_p75: f64,
    pub max_drawdown_median: f64,
    pub volatility_median: f64,
    pub win_rate: f64,
    pub regime_transition_prob: HashMap<String, f64>, // e.g. NO_TRADE -> RISK_ON
}
```

#### MVP 预定义条件

| 条件名 | 含义 |
|---|---|
| `srd-strong` | StrongBuy ≥ 5 且 StrategyState 保守 |
| `srd-moderate` | StrongBuy + Buy ≥ 3 且 StrategyState 保守 |
| `stretch-extreme-crowding-momentum` | Overall=Extreme，Crowding=Extreme，Momentum=Extreme，Breadth=Normal |
| `stretch-extreme-broad` | Overall=Extreme 且 Breadth 也 Extreme（全民疯牛型） |

#### CLI 命令

```bash
# 默认 horizon 20 交易日
cargo run -p quant-cli -- research-analytics --condition srd-strong --scope global

# 指定 horizon
cargo run -p quant-cli -- research-analytics --condition stretch-extreme-crowding-momentum --horizon 60 --scope global
```

输出示例：

```text
Conditional Forward Return Analytics | Condition: srd-strong | Scope: GLOBAL
================================================================================
  Occurrences:           23
  Horizon:               20 trading days
  Forward return median: +2.8%
  Forward return mean:   +3.1%
  Forward return P25:    -1.2%
  Forward return P75:    +6.5%
  Max drawdown median:   -2.4%
  Win rate:              65.2%
  Regime transition:     NO_TRADE -> RISK_ON: 34.8% | NO_TRADE -> NEUTRAL: 52.2%
================================================================================
Observation tool — does not influence any decision logic
```

#### 与 TASK-093 的关系

`memory/context.md` 中 TASK-093 要求系统性收集 StrongBuy + DE_RISK 背离样本，跟踪 T+20/T+60/T+120 收益。Analytics Layer 将成为 TASK-093 的技术承载层，自动完成样本收集与统计。

#### 风险与约束

- 必须遵守 Research Surface 边界：Analytics 输出**不进入** DashboardSnapshot、ResearchContext、Markdown Daily Report。
- 历史统计 ≠ 未来收益；输出必须附带 "Observation tool" 免责声明。
- 在 90 天 Shadow Production 结束前，统计结果只能用于人工研究和 ADR 准备，不能反向调整 State/Signal 阈值。

---

## 6. Phase C：90 天后

### 6.1 证据驱动的 ADR 流程

```
Any modification to Strategy State, Regime, Signal, or Execution requires an ADR.
```

只有在 Phase B 统计证据充分且稳定的前提下，才启动 ADR 讨论是否调整决策引擎。

### 6.2 预期可能的 ADR 议题（仅为占位，不预设结论）

- 若 SRD 持续 ≥5 天且后续 T+20 收益分布显著偏正 → ADR 讨论是否允许条件性参与
- 若 Market Stretch = Extreme 对顶部有稳定预测力 → ADR 讨论是否引入为辅助风控信号
- 若当前 Regime 在 SRD 期间的预警胜率 >70% → ADR 讨论是否维持当前权重

**当前阶段：不做任何预设。**

---

## 7. CLI 命名约定

统一使用二级命令结构，Research 工具归入 `research` 前缀：

```
quant-cli research srd              -- SRD 统计
quant-cli research stretch          -- 市场拉伸
quant-cli research symbol           -- 标的诊断（已有）
quant-cli research scoreboard       -- 标的记分板（已有）
quant-cli research rotation         -- 轮动排名（已有）
```

旧命令（`symbol-diagnostics`、`symbol-scoreboard`、`rotation-ranking`）保留为 alias，不破坏现有工作流。

---

## 8. Research Layer 长期演进方向

### 8.1 子层目录化（V6 后续或 V7）

```
research/
├── diagnostics/      — 解释现有决策
├── observe/          — 观测与证据收集（SRD、Stretch）
├── narrative/        — LLM 条件推演（Possible Scenarios）
└── explainability/   — 归因拆解
```

### 8.2 Research Layer 不能长成第二个 Quant Engine

- 永远不跨越到 Decision Layer
- 永远不创建信号、状态或执行规则
- 永远不基于短期观察自动修改系统参数

---

## 9. 前置依赖与约束

### 9.1 必须遵守的冻结纪律

| 层级 | 状态 | 说明 |
|------|------|------|
| State Layer | 🔒 冻结 | 策略状态逻辑不可修改 |
| Economic Layer | 🔒 冻结 | 经济预测层不可修改 |
| Signal Engine | 🔒 冻结 | 信号权重、阈值不可修改 |
| Execution Layer | 🔒 冻结 | 执行过滤逻辑不可修改 |
| Backtest 语义 | 🔒 冻结 | 回测逻辑和参数不可修改 |
| Production Surface | 🔒 冻结 | DashboardSnapshot、Markdown Report 展示逻辑不可修改 |

### 9.2 环境约束

- Eastmoney 不可达，全部标的走 Tencent fallback
- FRED 宏观因子依赖外部 API
- 静态 JSON 日历覆盖 2024-2027
- Wave 7.5 结论需在 `confirmation_days=1` 下重新验证
- Shadow Production 观察期至少 90 天

---

## 10. 与 V5 的边界

V6 **明确不做**（属于 V7 或更晚）：

- ❌ 修改任何 Quant Engine 决策逻辑
- ❌ 基于 SRD 或 Market Stretch 自动调整仓位
- ❌ 新增跨市场联动监控（违反 TASK-080 "少而精"原则）
- ❌ 新增风格切换 / Sector Flow Monitor（Rotation 已表达风格）
- ❌ 将 Research 工具的输出接入 Strategy State 或 Execution

V6 **在 V5 基础上新增**：
- ✅ SRD 统计工具（Observation 子层）
- ✅ Market Stretch 工具（Observation 子层）
- ✅ LLM 条件推演（Narratives 子层增强）
- ✅ Trust Summary 信息分层（UI 优化）
- ✅ Data Health / Coverage Bug 修复

---

## 11. 相关文档

- `docs/文档状态说明.md` — 当前实现主参考
- `docs/阶段性更新-2026-04-26.md` — 上一轮阶段性成果
- `memory/context.md` — 当前运行时状态
- `memory/decisions.md` — 架构决策记录
- `knowledge/` — KnowledgeGuard 静态资产库
