# 设计规划-v2

## 0. 实现状态更新（2026-04）

这份文档仍然保留 **V2 规划蓝图** 的作用，但其中一部分内容已经从“计划”进入“已落地”。

当前已完成的部分：

- `GLOBAL / CN / HK` 的 **per-scope market regime** 已落地
- `environment_snapshot` 已落地，包含 breadth / liquidity proxy / stress proxy / environment label
- dashboard / report / diagnostics 已按 scope 读取对应的 regime + environment
- `compute-macro` 已支持“最新抓取 + 已持久化宏观历史”合并重建，不再因单次 FRED 异常静默打空环境层

当前仍保留为 Phase 2+ 的部分：

- 双阶段策略状态机（`LEFT_PROBE / TREND_CONFIRM / FULL_TREND / DE_RISK`）
- 执行层仓位控制与回测联动
- 让 `strategy / signal / backtest` 真正消费 CN/HK scoped regime

因此阅读本文件时要区分：

> **环境层语义补全（Phase 1）已基本落地；策略层与执行层仍然主要处于规划阶段。**

### 开发执行规则

从当前版本开始，V2 的实现增加一条强约束：

> **所有开发 / 优化 / 修复性质的代码改动，都需要经过 Oracle 复核后，才能视为完成。**

这条规则适用于：

- 新功能实现
- 语义调整
- 架构/接口优化
- bug fix

原因不是流程形式化，而是为了避免：

- scope 语义继续漂移
- “看起来能跑”但用户会误解的实现
- 环境层 / 策略层 / 执行层之间出现隐性不一致

## 1. 规划目标

V1 已经完成了一个可用的本地桌面量化研究系统：

- 日线行情采集
- 指标计算
- 宏观 regime
- 轮动排名
- 策略偏好
- 最终信号
- 基础回测
- Dashboard / Markdown 报告
- 数据健康检查 / pipeline freshness 诊断
- GLOBAL / CN / HK scope 报告隔离

V2 的目标，不再是“把功能继续堆多”，而是：

> **把系统从“指数 / ETF 的趋势研究工具”升级成“可解释、可分层、可逐步扩展到更完整决策框架”的研究平台。**

也就是说，V2 不是追求大而全，而是围绕三件事升级：

1. **环境层更真实**：市场状态判断不再只是 global 粗粒度趋势
2. **策略层更完整**：把“左侧 + 趋势”双阶段框架真正落地
3. **执行层更有约束**：仓位 / 回撤 / 加减仓逻辑从概念进入可回测状态

---

## 2. 先给结论：哪些值得做，哪些先不做

## 2.1 当前最值得做（V2 必做）

### A. scope-aware market regime（已在 Phase 1 落地）

当前系统现在已经有：

- GLOBAL / CN / HK 的 scoped dashboard/report
- `market_regime` 已按 `GLOBAL / CN / HK` 输出
- `environment_snapshot` 已作为正式环境层输出

这意味着：

- CN/HK scope 不再只是日期隔离，而是环境解释也已本地化
- 但策略评分 / 最终 signal / 回测仍然沿用 `GLOBAL` regime

这部分已经从“V2 第一优先级”进入“V2 Phase 1 已实现基座”。

### B. “左侧 + 趋势”双阶段策略框架落地

你在 V2 规划中的核心想法是对的：

> 左侧负责低成本试探，趋势负责确认和加仓。

当前系统已有：

- regime
- breadth
- trend score
- rotation
- strategy preference

但这些还没有被组织成一个真正的“两阶段决策框架”。

这是第二个最值得落地的方向。

### C. 环境层指标补全：Breadth / Liquidity / Sentiment（第一版已落地）

当前环境层已有：

- 宏观因子
- anchor 趋势
- regime label
- breadth proxy
- liquidity proxy
- stress proxy
- environment label

但缺少更贴近交易解释的环境变量：

- Breadth momentum
- 流动性代理
- 情绪极端识别

这是让 V2 体系真正“像一个体系”的关键。

### D. 执行层的仓位 / 回撤控制原型

你在草案里提到：

- 小仓试探
- 趋势确认后加仓
- drawdown 超阈值自动降仓

这在当前系统里还只是概念，尚未真正成为：

- 可回测
- 可解释
- 可重复验证

应该成为 V2 的正式实现目标。

---

## 2.2 当前不建议优先做（延后）

### A. 个股全量系统

你当前 V2 草案里出现了大量“估值 / PE / PB / 股息率 / ERP / ROE / 净利润增长 / 行业龙头”式的个股框架。

从策略思想上它是合理的，但从当前 repo 实际状态看：

- 现在 universe 仍是 INDEX / ETF 主体
- 没有股票 instrument type 主链路
- 没有股票级基本面数据模型
- 没有个股级 provider / 历史清洗 / 指标 / 信号 / 风险闭环

所以：

> **个股全量价值投资系统，不应该作为当前 V2 的主目标。**

它更像 V2.5 / V3 话题。

### B. 复杂执行模拟（真实撮合 / 多账户 / 自动交易）

当前系统定位是：

- 本地研究
- 低频
- 辅助判断

所以：

- 实盘撮合
- 券商接口
- 自动下单
- 多账户
- 高频/准实时

都不应该进入当前 V2。

### C. 过早拆成大量 crate / 服务化

当前 repo 的痛点确实包括：

- `app-service` 偏大
- `market-store` 偏大

但这不意味着 V2 一开始就要做大拆分。

更合理的是：

> **先按“环境层 / 策略层 / 执行层”做内部模块分层，再决定是否抽 crate。**

---

## 2.3 当前没必要做（至少这一版先不做）

- 多用户权限系统
- Web 化改造
- 云端服务化
- 高频/分钟级策略
- 实盘自动执行
- 全量个股基本面平台
- 复杂组合优化器（因子暴露、协方差矩阵、风险预算优化）

这些都和当前产品定位不一致。

---

## 2.4 从重度用户视角看，V2 的真实实现优先级

如果从“项目作者 + 高频重度使用者”的角度看，V2 现在最需要优化的不是再堆功能，而是先补三件事：

1. **信任感**：用户看到的 scope、导出的报告、signal、backtest 语义必须一致
2. **研究闭环**：刷新完成后，要立刻知道“这次数据能不能信、值不值得继续分析”
3. **研究效率**：高频使用时，不该总在重复切 scope、找报告、自己反推 signal 原因

因此，V2 的真实优先级应按下面顺序推进：

### P0：先补 scope / signal / backtest 的信任缺口

### 当前问题

现在已经成立的是：

- dashboard / report 的 `GLOBAL / CN / HK` 已经是 scoped regime + scoped environment

但仍未成立的是：

- `strategy_preference` 仍读取 `GLOBAL` regime
- `signal_snapshot` 仍读取 `GLOBAL` regime
- `backtest` 仍消费全局 signal 流

这会形成最危险的用户认知落差：

> **我看到的是 CN/HK 环境解释，但拿到的 signal/backtest 仍然是 GLOBAL 语义。**

### 这一优先级要做什么

1. **短期信任补丁**
   - dashboard / report / export 明确标出：
     - 当前 signal 是否仍为 `GLOBAL-regime scored`
     - 当前 backtest 是否仍为 `global-only`
2. **中期语义打通**
   - 把 `AnalysisScope` 扩进：
     - `compute_strategy_preferences()`
     - `compute_signals()`
     - backtest 输入链路

### 为什么是 P0

如果这一步不先做，后面的状态机、仓位建议、执行层原型都会建立在一个用户难以完全信任的解释层上。

---

### P1：把“刷新 → 校验 → 使用”做成可信研究闭环

### 当前问题

现在系统能刷新整条链路，但刷新完成后，用户仍要自己再判断：

- pipeline freshness
- latest-day completeness
- macro source 是否 degraded
- 当前结果是否适合继续分析 / 导出 / 复盘

也就是说：

> **系统会刷新，但不会在刷新结束时直接告诉用户“这次结果能不能信”。**

### 这一优先级要做什么

1. refresh 完成态直接附带：
   - pipeline freshness
   - latest-day completeness
   - macro source status
   - 关键 data-health warnings
2. report 导出时附带：
   - freshness
   - coverage
   - degraded source 注释
   - 当前 signal / backtest 的 regime basis 注释
3. backtest 展示补 provenance：
   - run date
   - scope
   - signal date range
   - config
   - 是否与当前 snapshot 匹配

### 为什么是 P1

对重度用户来说，日常高频动作其实是：

> 刷新 → 判断这次数据是否可信 → 决定是否继续分析/导出/复盘。

这一步如果不顺，整个系统更像一个 demo，而不是研究工具。

---

### P2：提升解释能力与研究深挖能力

### 当前问题

现在 dashboard 已经有总览层，但缺少重度用户最常用的深入动作：

- signal 为什么触发的结构化解释
- symbol drilldown
- 历史对比
- 更强的 report / signal / rotation 追踪入口

### 这一优先级要做什么

1. 把 signal explanation 从调试式字符串升级成结构化原因：
   - best strategy
   - alignment
   - regime basis
   - rotation rank
   - MA/MACD/趋势关键状态
2. 引入 symbol drilldown / detail view：
   - 单标的趋势结构
   - 历史 signal 变化
   - rotation 历史位置
   - 当前策略评分分解
3. 让 diagnostics 更接近当前 scope：
   - provider 状态可继续全局
   - 但 latest-day coverage / symbol review queue 最好支持 scope-aware 展示

### 为什么是 P2

解释能力和 deep dive 的价值很高，但必须建立在前两层之上：

- 如果 scope 语义还不一致，解释越强，误导越深
- 如果刷新结果本身还不够可信，drilldown 只是把不确定性看得更细

---

### P3：优化高频操作效率

### 当前问题

现在已经存在一些“能用但不够顺手”的点：

- `ui_preferences` 已有表，但 scope/date 等偏好还没真正固化到体验层
- recent reports 只显示路径，不够像“研究结果管理”
- refresh 仍然是“一整条链跑完”，没有 partial rerun / cancel / retry failed stage

### 这一优先级要做什么

1. 持久化用户偏好：
   - selected scope
   - selected report date / latest behavior
   - guide viewer / diagnostics 折叠状态
2. recent reports 增强：
   - open artifact
   - copy path
   - compare with previous
3. refresh 操作增强：
   - cancel
   - retry failed stage
   - partial rerun（只重跑 macro / signal / report）

### 为什么是 P3

这一层会显著提升重度使用体验，但属于：

> **在“可信、可解释”已经成立之后，再去优化“更顺手”。**

---

### P4：性能优化是后置项，不是当前主线

可以优化的点当然存在，例如：

- ingest 串行化
- refresh 多阶段串行
- 更细粒度并行
- 更丰富的进度反馈

但当前项目的主要风险并不是“慢一点”，而是：

> **用户是否会误读结果，是否能快速判断这次分析能不能信。**

所以性能优化的正确顺序是：

- 先补语义一致性
- 再补研究闭环
- 再补解释与 drilldown
- 最后再做高 ROI 的性能与操作便捷性改造

---

## 3. V2 的正确定位

V2 不应该理解成“从指数 / ETF 系统切去做个股价值系统”。

更合理的定位是：

> **在现有指数 / ETF 研究框架上，补齐环境、策略、执行三个层次，让系统真正能表达“什么时候试探、什么时候确认、什么时候控制风险”。**

也就是：

- 不是推翻 V1
- 而是在 V1 上做体系化升级

---

## 4. V2 的总体架构

## 4.1 三层架构（保留你原来的核心思想）

### 1）环境层 Environment Layer

回答：

> **现在能不能做？能做多大？**

包含：

- macro regime
- breadth
- liquidity proxy
- sentiment / stress
- scope-aware market regime（GLOBAL / CN / HK）

### 2）策略层 Strategy Layer

回答：

> **当前更适合左侧试探还是趋势确认？**

包含：

- left-entry 条件
- trend-confirm 条件
- breadth confirmation
- rotation filter
- strategy score / state machine

### 3）执行层 Execution Layer

回答：

> **这次应该配多少仓，什么情况下减仓？**

包含：

- position sizing
- staged entry / staged add
- drawdown control
- de-risk rules
- backtest execution rules

---

## 5. 基于当前 repo 的 V2 核心模块设计

## 5.1 环境层升级（必须做）

### 5.1.1 scope-aware market regime

### 当前状态（已落地）

- dashboard/report 已支持 `GLOBAL / CN / HK` scope
- `market_regime` 已输出 `GLOBAL / CN / HK`
- `environment_snapshot` 已落地并进入 dashboard/report
- diagnostics 已按 scope 检查 regime / environment 完整性

### 当前边界

- `strategy_preference` 仍读取 `GLOBAL` regime
- `signal_snapshot` 仍读取 `GLOBAL` regime
- `backtest` 仍消费全局 signal 流

### V2 目标

把 `market_regime` 扩展为：

- `GLOBAL`
- `CN`
- `HK`

每个 scope 独立输出：

- `trend_score`
- `liquidity_score`
- `risk_score`
- `regime_label`
- `macro_as_of_date`

### 已落地实现

- `macro-engine` 已输出 per-scope regime rows
- `app-service` / `report-engine` / desktop 已按 scope 选择对应 regime
- `market-store` 已支持 scoped regime/environment 查询

### 当前结论

这部分已不再是“语义近似点”，而是 V2 Phase 1 已完成事项。

---

### 5.1.2 breadth 升级为正式环境因子

### 当前状态（已落地第一版）

- 已有 watchlist breadth proxy
- 已有 breadth delta / completeness / latest-day diagnostics
- 已正式写入 `environment_snapshot`

### 已落地结果

breadth 已从“辅助观察”提升为环境层正式输入，当前已输出：

- `breadth_pct`
- `breadth_pct_sma5`
- `breadth_5d_delta`
- `breadth_state`

### 当前口径

- 仍然先用 watchlist breadth proxy
- 已把它正式写入 environment layer 评分中

### 暂不做

- 全市场个股 breadth

因为这需要股票 universe 扩展。

---

### 5.1.3 liquidity / stress proxy（第一版已落地）

当前第一版已经落地的轻量变量：

- volume / turnover participation proxy
- liquidity proxy score
- FRED stress 组合衍生 score
- environment label

后续仍不建议一开始就追求太多变量。

---

## 5.2 策略层升级（V2 核心）

## 5.2.1 双阶段交易框架正式化

### 阶段 A：左侧试探

触发条件建议：

- regime 不处于极端 risk_off 崩塌态
- breadth 低位 / 修复初期
- 超跌（RSI / 偏离率 / drawdown proxy）
- 估值 / 情绪 proxy（在 ETF/指数层用可替代代理）

输出：

- `LEFT_PROBE`
- 建议仓位 10%~30%

### 阶段 B：趋势确认

触发条件建议：

- TrendScore 提升
- MA60 重新站上
- breadth > 某阈值
- rotation 排名靠前

输出：

- `TREND_CONFIRM`
- 建议仓位提升到 60%~80%

### 为什么值得做

这正是你 V2 草案最有价值的部分，而且它**能利用当前 repo 已有数据立即落地**。

---

## 5.2.2 策略状态机

建议引入统一状态而不是只有散乱评分：

- `NO_TRADE`
- `LEFT_PROBE`
- `CONFIRM_ADD`
- `FULL_TREND`
- `DE_RISK`

这样：

- dashboard 可解释性更强
- report 更容易阅读
- backtest 也更容易验证

---

## 5.3 执行层升级（必须有，但先做原型）

## 5.3.1 position sizing prototype

不是直接上复杂优化，而是先做：

- base position size
- add-on size
- max exposure
- de-risk scale-down

例如：

- `LEFT_PROBE` → 20%
- `CONFIRM_ADD` → 50%
- `FULL_TREND` → 80%
- `DE_RISK` → 30%

---

## 5.3.2 drawdown control prototype

建议先加简单而有效的规则：

- strategy-level drawdown threshold
- regime shock threshold
- breadth collapse threshold

触发：

- 自动降低仓位建议
- 在报告里给出 risk action

---

## 6. V2 不应该直接做的内容

## 6.1 个股估值 / 基本面系统

### 原因

- 当前 repo 没有 stock universe 主链路
- 没有 fundamentals 存储与更新机制
- 没有个股级清洗与信号闭环

### 结论

这块可以保留在 `设计规划-v2.md` 作为长期方向，但不要当成当前 V2 的正式交付目标。

---

## 6.2 真实交易执行系统

当前定位仍是研究系统，不应扩成：

- broker integration
- auto order
- live execution engine

---

## 6.3 高级组合优化器

例如：

- 风险预算
- 协方差矩阵优化
- 因子暴露约束

这些都不适合作为当前 V2 第一阶段。

---

## 7. 推荐的 V2 实施步骤

## Phase 1：环境层语义补全（优先级最高）

### 当前状态：已基本完成

目标：

- per-scope regime
- breadth environment integration
- liquidity/stress proxy

输出：

- `GLOBAL / CN / HK` regime 全部可用
- dashboard/report 中 scope 与 regime 语义一致
- `environment_snapshot` 正式可用

验收：

- `dashboard-snapshot --scope cn/hk/global` 的 regime 都是对应 scope 语义
- 不再复用 global regime 假装本地化

### 已知保留项

- strategy / signal / backtest 仍沿用 `GLOBAL` regime
- Phase 1 重点是环境层和解释层，不是策略状态机

---

## Phase 2：先补信任闭环，再进入双阶段策略框架

### 当前优先级

这一阶段不应该一上来就直接做状态机，而应该先完成：

1. scope 语义信任补丁
2. refresh / export / diagnostics 的可信闭环
3. backtest provenance

也就是说：

> **先让用户“敢信”，再让系统“更复杂”。**

### Phase 2A：信任闭环

目标：

- 明确标注 signal/backtest 当前的 regime basis
- 让 refresh 完成即附带 freshness / health judgement
- 让 report/backtest 自带 provenance

输出：

- scope/regime basis 清晰可见
- refresh 完成后即可判断“这次结果能不能用”
- report / backtest 可追溯

验收：

- CN/HK dashboard/report 中，不再让用户误以为 signal/backtest 已完全 scoped
- export markdown 中包含 freshness / health / basis 说明
- latest backtest 能看出是否与当前 snapshot 匹配

### Phase 2B：双阶段策略框架

目标：

- 正式引入 `LEFT_PROBE / TREND_CONFIRM / FULL_TREND / DE_RISK`

输出：

- strategy state machine
- report / dashboard 状态说明
- 对应仓位建议字段

验收：

- 在相同数据上能够生成稳定且可解释的状态切换
- report 中能明确说明“为什么当前是试探 / 确认 / 降仓”

---

## Phase 3：解释增强 + 执行层原型 + 回测联动

### 先做解释增强

目标：

- signal explanation 结构化
- symbol drilldown / 历史解释能力
- scope-aware diagnostics 呈现

输出：

- 从“总览 dashboard”升级成“可深挖的研究台”

验收：

- 用户可以从 signal 直接看见策略分解与关键原因
- drilldown 可用于解释“为什么是它、为什么是现在”

### 再做执行层原型

目标：

- 把仓位建议接入回测
- 引入 drawdown control 原型

输出：

- execution-aware backtest
- staged entry / staged add / de-risk 规则

验收：

- 回测结果能体现不同状态下的仓位差异
- risk rules 可解释、可复现

---

## Phase 4：诊断与可解释性补强

目标：

- 让 V2 结果更透明、更可调试

输出：

- strategy decision breakdown
- environment decomposition
- scope-aware diagnostics

验收：

- 任意一个报告结论都能追溯回环境层 + 策略层 + 执行层的依据

---

## 7.1 每个阶段的可执行验收方式

### Phase 1：环境层语义补全

验收命令建议：

- `cargo run -p quant-cli -- dashboard-snapshot --scope global`
- `cargo run -p quant-cli -- dashboard-snapshot --scope cn`
- `cargo run -p quant-cli -- dashboard-snapshot --scope hk`

验收目标：

- scoped report/dashboard 使用对应 scope 的 regime
- 不再只是 global regime 的近似复用

### Phase 2：信任闭环 + 双阶段策略框架

验收方式：

- 先执行 P0-A provenance 验收
- 先检查 scoped dashboard / report / backtest provenance
- 再选 3 组历史窗口：弱势 / 修复 / 强势
- 重跑完整链路并导出报告

验收目标：

- 用户能明确知道当前 signal/backtest 的 scope basis
- refresh / export 结果附带 freshness / health / provenance
- 能输出清晰的 `LEFT_PROBE / CONFIRM_ADD / FULL_TREND / DE_RISK`
- 报告中能解释状态切换原因

#### P0-A provenance contract 验收

建议命令：

- `cargo run -p quant-cli -- init-storage`
- `cargo run -p quant-cli -- dashboard-snapshot --scope cn --date 2026-04-01`
- `cargo run -p quant-cli -- export-report --scope cn --date 2026-04-01`

验收目标：

- `dashboard-snapshot` 中的 `top_signals / bullish_signals / defensive_signals` 含有 provenance 字段
- `latest_backtest` 若存在，能够看见 scope / signal window / config summary
- markdown report 中明确标注 signal/backtest 的 basis 与是否匹配当前 snapshot

### Phase 3：解释增强 + 执行层原型

验收方式：

- 先验证 signal 结构化解释 / symbol drilldown
- 再对比“启用仓位规则”和“禁用仓位规则”的回测结果

验收目标：

- 用户能直接读懂 signal 由哪些因素构成
- 仓位变化可解释
- drawdown control 能真实改变回测路径

### Phase 4：可解释性与诊断

验收方式：

- 用 dashboard / report / pipeline-dates 交叉验证

验收目标：

- 任意一个结论都能回溯到具体环境输入、策略状态和执行动作

---

## 8. 推荐的“当前就做 / 暂缓”清单

## 8.1 当前就做

1. **P0：scope 信任缺口补齐**
   - signal / backtest regime basis 显式化
   - scoped strategy/signal/backtest 改造路径确定
2. **P1：刷新/导出/回测可信闭环**
   - refresh 完成态附带 freshness + health judgement
   - report / backtest provenance
3. **P2：双阶段策略状态机**
4. **P2：结构化 signal 解释 + symbol drilldown**
5. **P3：position sizing 原型**
6. **P3：drawdown control 原型**
7. **P3：高频操作效率增强**
   - preference persistence
   - recent reports 可操作化
   - refresh partial rerun / cancel / retry

### 说明

- 这里的优先级顺序高于“想做的功能列表”
- 如果前面的信任闭环没补齐，后面的状态机、执行层与性能优化都会变得难以解释和难以验证

### 已完成的前置基座

1. per-scope regime
2. breadth environment integration
3. liquidity / stress proxy

## 8.2 按模块拆分的可执行任务清单

下面这份清单不是“愿景描述”，而是可以直接进入 backlog 的执行拆分。

### P0-A：先补 provenance contract 与信任标注

#### 目标

先让用户明确知道：

- 当前 signal 用的是什么 regime basis
- 当前 backtest 对应什么 scope / 日期范围 / 配置
- 当前 report 是否可以被当成可信研究结果引用

#### 模块级任务

1. **领域模型 / DTO**
   - `crates/core-domain/src/lib.rs`
   - `crates/backtest-engine/src/lib.rs`
   - 为 `StrategyPreferenceSnapshot`、`SignalSnapshot`、`BacktestSummary` 设计 provenance 字段
   - 至少包括：
     - `analysis_scope`
     - `regime_basis_scope`
     - `signal_source_range`
     - `backtest_config_summary`
2. **存储与 schema**
   - `sql/clickhouse/001_init.sql`
   - `crates/market-store/src/lib.rs`
   - 为 strategy / signal / backtest 持久化 provenance 字段并补查询接口
3. **编排与聚合**
   - `crates/app-service/src/lib.rs`
   - 把 provenance 注入 dashboard / export / recent backtest 载荷
4. **展示与文案**
   - `crates/report-engine/src/lib.rs`
   - `apps/desktop/src-tauri/src/lib.rs`
   - `apps/desktop/frontend/src/main.js`
   - `apps/cli/src/main.rs`
   - 显式标出：`GLOBAL-regime scored` / `scope-aware` / `backtest matches current snapshot`

#### 依赖关系

- 必须先有 DTO/schema，才能做 UI/report 标注

---

### P0-B：再把 scoped strategy / signal / backtest 真正打通

#### 目标

让 `GLOBAL / CN / HK` 不只是 dashboard/report scoped，而是评分、signal、backtest 也真正 scoped。

#### 模块级任务

1. **策略层输入改造**
   - `crates/strategy-engine/src/lib.rs`
   - `crates/app-service/src/lib.rs`
   - 让 `compute_strategy_preferences()` 接收 `AnalysisScope`
2. **signal 层输入改造**
   - `crates/signal-engine/src/lib.rs`
   - `crates/app-service/src/lib.rs`
   - 让 `compute_signals()` 按 scope 消费 scoped regime / scoped strategy preference
3. **backtest 输入改造**
   - `crates/backtest-engine/src/lib.rs`
   - `crates/app-service/src/lib.rs`
   - 让 backtest 可明确基于 `GLOBAL / CN / HK` signal 流运行
4. **CLI / desktop surface**
   - `apps/cli/src/main.rs`
   - `apps/desktop/src-tauri/src/lib.rs`
   - 为 backtest / strategy / signal 相关入口补 scope 参数与默认兼容路径

#### 依赖关系

- 这一组必须建立在 P0-A 的 provenance contract 已存在之上
- 默认 `GLOBAL` 兼容路径必须保留，直到 scoped path 验证完成

---

### P1：把 refresh / export / diagnostics 收敛成可信研究闭环

#### 目标

让用户在 refresh 完成时立刻知道：

- 数据是否完整
- provider 是否 degraded
- 当前 snapshot 是否适合导出 / 复盘 / 继续研究

#### 模块级任务

1. **统一 trust summary 聚合**
   - `crates/app-service/src/lib.rs`
   - 把 `PipelineDateDiagnostics` + `DataHealthSummary` + provenance 收敛成一个统一 trust judgement
2. **刷新完成态增强**
   - `apps/desktop/src-tauri/src/lib.rs`
   - `apps/desktop/frontend/src/main.js`
   - refresh success 直接附带 freshness / completeness / degraded source / trust verdict
3. **报告可信上下文**
   - `crates/report-engine/src/lib.rs`
   - `crates/app-service/src/lib.rs`
   - markdown report 顶部增加 freshness / health / basis / caveat 区块
4. **回测 provenance 展示**
   - `crates/report-engine/src/lib.rs`
   - `apps/desktop/frontend/src/main.js`
   - latest backtest 附带 scope / date range / config / current snapshot match status

#### 依赖关系

- 这一组依赖 P0-A 的 provenance contract
- trust judgement 要先在 `app-service` 聚合，再下发给 report/frontend，避免重复实现规则

---

### P2：结构化解释 + symbol drilldown

#### 目标

让系统从“总览 dashboard”升级成“可深挖的研究台”。

#### 模块级任务

1. **signal 原因模型结构化**
   - `crates/core-domain/src/lib.rs`
   - `crates/signal-engine/src/lib.rs`
   - 用结构化 reason model 替代当前 explanation string 主导的模式
2. **detail payload / symbol 视图 contract**
   - `crates/report-engine/src/lib.rs`
   - `crates/app-service/src/lib.rs`
   - 设计 symbol detail payload：
     - 策略分解
     - trend structure
     - regime/environment context
     - rotation / signal 历史摘要
3. **桌面端 drilldown UI**
   - `apps/desktop/src-tauri/src/lib.rs`
   - `apps/desktop/frontend/src/main.js`
   - 为 signal / rotation / report 列表增加 click-through detail view
4. **scope-aware diagnostics 呈现**
   - `crates/app-service/src/lib.rs`
   - `apps/desktop/frontend/src/main.js`
   - provider 状态继续全局，symbol review queue / latest-day coverage 支持按 scope 看

#### 依赖关系

- 需要 P0 / P1 先把语义与 trust 讲清楚
- 否则 drilldown 只会把不一致解释得更复杂

---

### P3：高频操作效率增强

#### 目标

降低重度用户的重复操作成本。

#### 模块级任务

1. **偏好持久化**
   - `sql/sqlite/001_init.sql`
   - `crates/market-store/src/lib.rs`
   - `crates/app-service/src/lib.rs`
   - `apps/desktop/src-tauri/src/lib.rs`
   - `apps/desktop/frontend/src/main.js`
   - 持久化 selected scope / selected date / latest behavior / viewer state
2. **recent reports 可操作化**
   - `apps/desktop/src-tauri/src/lib.rs`
   - `apps/desktop/frontend/src/main.js`
   - 增加 open / copy path / compare previous 等动作
3. **refresh 控制增强**
   - `apps/desktop/src-tauri/src/lib.rs`
   - `crates/task-runner`
   - `apps/desktop/frontend/src/main.js`
   - 支持 cancel / retry failed stage / partial rerun

#### 依赖关系

- 偏好持久化和 recent reports 可操作化可以先做
- refresh partial rerun / cancel / retry 属于较重任务，应在前面几组稳定后再做

---

### 建议执行顺序（模块视角）

1. provenance DTO / schema
2. provenance store / app-service aggregation
3. dashboard/report/export basis 标注
4. trust summary 聚合与 refresh 完成态
5. scoped strategy / signal / backtest 真正打通
6. structured signal reason model
7. symbol drilldown
8. preference persistence / report actions
9. refresh control增强

## 8.3 暂缓

1. 个股基本面 / 价值系统
2. 自动交易执行
3. 复杂组合优化器
4. 全面 crate 大拆分
5. Web / 云端 / 多用户化

---

## 9. 最终建议

### 结论 1

你在 `设计规划-v2.md` 里的“左侧 + 趋势”思想是对的，应该保留为 V2 核心。

### 结论 2

但当前 V2 应该围绕：

- 环境层真实化
- 策略层状态机化
- 执行层原型化

而不是把大量个股估值系统一起塞进来。

### 结论 3

最适合当前 repo 的 V2，不是“做更大”，而是：

> **让现在的指数 / ETF 研究系统从“能看”升级到“能解释、能分阶段决策、能在不同市场 scope 下语义一致”。**

---

## 10. Oracle 复核要点（本规划的关键约束）

本 V2 规划经过当前代码状态约束后的核心判断是：

- **应该做**：per-scope regime、双阶段策略、执行层原型
- **不应该急着做**：个股全量价值系统、自动交易、复杂优化器
- **当前架构可承接**：环境层 / 策略层 / 执行层先在现有 repo 中做模块化演进
- **不建议**：为了“看起来高级”先做大规模重构

一句话总结：

> **V2 的最佳方向不是“扩品类”，而是“提升层次与一致性”。**
