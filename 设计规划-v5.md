# V5 设计规划：研究记忆与认知演进

> 本文件用于持续记录 V5 阶段的设计规划、技术决策与实施方向。
> 创建日期：2026-06-15
> 前置阶段：V4（Research Cognition Layer）已落地，详见 `设计规划-v4.md` 与 `docs/阶段性更新.md#2026-06-02`
> 当前状态：v4.5 已发布，进入 Validation Sprint 阶段。TASK-120 Execution Layer Foundation 已落地（2026-06-22），前端 UI 进入精细化打磨阶段（SignalsPanel 悬浮详情卡片已交付）。MemGuard 任务状态审计完成。

---

## 1. 目标概述

V5 的核心目标不是扩展更多分析技能，而是：

> **构建研究记忆系统——让"过去的分析"变得可检索、可比较、可追踪，从而实现认知演进。**

完成从：
```
"每次分析都是独立的"
```
到：
```
"分析有记忆、状态有历史、技能有演化"
```
的范式转换。

---

## 2. 核心概念

### 2.1 研究记忆（Research Memory）

将 V4 的 `ResearchAnalysis` 输出持久化到结构化存储中，形成可追溯的研究历史：

- **Regime Transition History**：记录每一次 regime 状态切换的时间、触发条件、分析结论
- **Analysis Memory**：保存每次 Skill 分析的完整输入（ResearchContext）和输出（ResearchAnalysis）
- **Skill Evolution Tracking**：记录 Skill 定义变更（版本升级）、效果对比、性能趋势
- **Agent Performance Ledger**：不同 Agent Profile 在不同市场环境下的表现追踪

### 2.2 记忆驱动的增强分析

基于历史记忆，在 V4 的 Skill 分析基础上增加：

- **历史对比**："当前 regime 与 2024-03 那次 risk_off 切换有何异同？"
- **模式识别**：从 regime transition history 中提炼高频模式
- **预测校准**：基于历史预测准确率，动态调整当前分析的置信度
- **技能推荐**：基于历史表现，推荐当前市场环境下最有效的 Skill 组合

---

## 3. 待探索方向（占位）

以下方向尚未进入详细设计，仅作为 V5 的候选议题：

| 方向 | 当前状态 | 说明 |
|------|----------|------|
| Regime 状态机历史 | 概念阶段 | 基于 V4 的 `RegimeTransition` 结构建立历史数据库 |
| 分析记忆持久化 | 概念阶段 | 将 `ResearchAnalysis` 存入 SQLite/ClickHouse，支持检索 |
| 技能演化追踪 | 概念阶段 | Skill 版本变更的 benchmark 对比与趋势分析 |
| Agent 表现账本 | 概念阶段 | 不同 Agent 在不同市场环境下的长期表现追踪 |
| 历史对比分析 | 概念阶段 | 基于记忆的相似 regime 检索与对比分析 Skill |
| 预测校准系统 | 概念阶段 | 根据历史预测准确率动态调整置信度 |
| 市场模式库 | 概念阶段 | 从 regime history 中提炼可命名的市场模式 |

---

## 4. 前置依赖

V5 必须等待以下前置条件完成：

1. **V4 Wave 3 完成**：至少 1 个完整 Skill 链路（`market-regime-reasoning`）打磨稳定
2. **V4 Benchmark 建立**：`research-benchmark` 能够产出可比较的基准数据
3. **Wave 9 完成**：当前 `shadow_production` 阶段（ADR-060）的 Ground Truth 重新定义完成
4. **TASK-080 系列完成**：10 因子 regime 系统稳定运行并积累足够历史数据

---

## 5. 与 V4 的边界

V5 **明确不做**（属于 V6/V7 范畴）：
- ❌ Multi-Agent 编排（V6）
- ❌ Consensus Engine（V6）
- ❌ Marketplace / 第三方 Skill（V7）
- ❌ 企业权限系统（V7）

V5 **明确做**（记忆层基础设施）：
- ✅ 分析历史持久化
- ✅ Regime 切换历史追踪
- ✅ 基于历史的对比分析 Skill
- ✅ 技能版本效果追踪
- ✅ Agent 长期表现账本

---

## 6. 持续记录区

> 以下区域用于追加后续的设计决策、技术讨论、实施计划。

### 2026-06-15 — 初始创建
- 文件初始化
- 基于 V4 长期演进路线（`设计规划-v4.md` 第 10 节）定义 V5 核心方向：Research Memory
- 等待 V4 前置条件完成后再进入详细设计

### 2026-06-22 — Execution Layer Foundation（TASK-120，A类基础设施）

本轮新增属于 **A类（基础设施）**，不触碰任何 B类禁止项。物理隔离为新 crate，单向消费现有信号，不修改冻结层。

#### 背景与定位

在 Shadow Production 观察期间，系统已经解决了「方向看得准不准」的问题（State + Signal + Rotation 冻结）。下一阶段真正影响收益和体验的，是：

> 「方向看对以后，什么时候下手」

因此新增 **Execution Layer**，定位为 **Pattern Library（经验现象库）**，而非 Timing Engine 或 Intraday Signal Engine。

**ADR-070 核心契约**：Execution Layer answers "When", not "What". 物理隔离为 `execution-engine` crate，单向消费现有信号，不修改任何冻结层。

**ADR-072 演进契约**：Execution Layer evolves into a Pattern Library that filters existing ideas. 它观察市场现象（price action, volume, position）并映射到执行指引，不创建新投资想法或评分系统。

**ADR-071 观测禁令**：90 天内仅允许作为观测/诊断工具运行，禁止声称性能优势（Sharpe/CAGR）、禁止优化参数、禁止 Backtest v2。

---

#### 五层架构

```
Macro Layer
    ↓
State Layer       ← 冻结（hard gate）
    ↓  state ≠ NO_TRADE
Signal Layer      ← 冻结
    ↓  signal ≥ Buy
Execution Layer   ← 新增（Pattern Library, soft gate）
    ↓
Order
```

**关键原则**：State 是 hard gate，Signal 是候选 gate，Execution 是 soft gate。Execution 永远不能挑战 State。

---

#### 核心类型

```rust
pub enum ExecutionState {
    BuyNow,   // 立即执行
    Wait,     // 分析了，无 pattern 匹配
    NoChase,  // 不追
    Reduce,   // 减仓
    Skip,     // 根本没进入分析
}

pub enum ReasonTag {
    GapUpOverextended,
    VolumeSpike,
    FarFromMA5,
    DistributionDay,
    VolumeSurgeDecline,
    StrongClose,
    HighVolume,
    DataUnavailable,   // 腾讯 API 失败
    StateGate,         // NO_TRADE 硬 gate
}

// 内部使用，不暴露给外部 API
enum SkipReason {
    NoCandidate,
    StateGate,
    DataUnavailable,
}

pub struct ExecutionDecision {
    pub symbol: String,
    pub state: ExecutionState,
    pub reasons: Vec<ReasonTag>,
}
```

**注意**：`ExecutionDecision` 不持有 `SignalContext`。需要 context 时运行时 join，避免形成第二份 snapshot。

---

#### 3 个 Pattern（Phase1）

所有 Pattern 直接写在 `engine.rs` 中，不拆 `patterns/` 目录（Pattern ≥ 8 时再拆）。

| Pattern | 触发条件 | 输出 | 标签 |
|---------|---------|------|------|
| **NoChase** | `today_return > 2% && distance_ma5 > 4% && volume_ratio > 1.8` | `NoChase` | `GapUpOverextended`, `VolumeSpike`, `FarFromMA5` |
| **Distribution** | `today_return < -1.5% && volume_ratio > 1.5 && close_position < 0.2` | `Reduce` | `DistributionDay`, `VolumeSurgeDecline` |
| **StrongClose** | `today_return > 1% && close_position > 0.8 && volume_ratio > 1.3` | `BuyNow` | `StrongClose`, `HighVolume` |

**无匹配 → 默认 `Wait`**

---

#### 候选过滤

```
signal ≥ Buy
∩ state ≠ NO_TRADE
```

**rotation 不参与 Execution 过滤**。rotation 已参与 Signal 的形成，Execution 再过滤 `rotation≤10` 属于双重过滤（double gate），会让 Execution 偷偷参与 "What"。

候选数量通常为 8~12 个，Execution 全部分析。

---

#### 数据获取与降级

| 数据项 | 来源 | 失败降级 |
|--------|------|---------|
| 当前价/开盘价/最高/最低/成交量/昨收 | **Tencent API** (`qt.gtimg.cn`) | `Skip(DataUnavailable)` |
| MA5 / 20日均价 | ClickHouse 本地 `daily_bar` | 已有数据 |

**关键原则**：实时数据故障永远不能影响主系统。Signal Layer 继续正常，Execution 降级为 `Skip`。

---

#### 物理结构

```
crates/execution-engine/
├── Cargo.toml
└── src/
    ├── lib.rs
    ├── types.rs           # ExecutionState, ExecutionDecision, ReasonTag, SkipReason, IntradaySnapshot
    ├── fetcher.rs         # Tencent API 批量快照（失败 → Skip）
    └── engine.rs          # 3 Pattern 函数 + 主编排逻辑
```

**无 `patterns/` 目录**（3 个函数不值得拆文件）。

**无 SQLite / ClickHouse 新表**（25 rows/day，纯内存）。

---

#### CLI 输出示例

```bash
$ cargo run -p quant-cli -- preclose-analysis --scope cn

Scope: CN
Date: 2026-06-22
Candidates: 8 (filtered: signal≥Buy ∩ state≠NO_TRADE)

Symbol    Signal      State    Reasons
515880    StrongBuy   SKIP     StateGate
512480    StrongBuy   BUY_NOW  StrongClose, HighVolume
000688    Buy         WAIT     (no pattern match)
510300    StrongBuy   NO_CHASE GapUpOverextended, VolumeSpike, FarFromMA5
159915    Buy         SKIP     DataUnavailable
```

---

#### Phase2 JSON Sample Library（90 天观测）

目录：`reports/execution-samples/`

文件名：`2026-06-22.json`

格式：

```json
[
  {
    "symbol": "515880",
    "signal": "StrongBuy",
    "execution_state": "NoChase",
    "reasons": ["GapUpOverextended", "VolumeSpike"],
    "snapshot": {
      "today_return": 2.6,
      "volume_ratio": 2.1,
      "close_position": 0.94,
      "distance_ma5": 0.05
    }
  }
]
```

**必须记录 snapshot**：90 天后分析的不是 "BUY_NOW 是否赚钱"，而是 "Pattern 出现时的市场特征"。否则丢失证据。

**禁止（ADR-071）**：
- ❌ Backtest v2
- ❌ Sharpe / CAGR 声称
- ❌ 参数优化
- ❌ 新增 Pattern

---

#### Phase3 候选 Pattern（90 天后评估）

根据旧系统 `preclose_decision.py` 的二次验证经验，以下 Pattern 值得未来扩展：

| 旧规则 | 未来 Pattern | 触发条件 | 输出 | 优先级 |
|--------|-------------|---------|------|--------|
| R1: 买在无人问津时，卖在人声鼎沸处 | **CrowdedTrade** | 低动量+缩量 vs 高动量+放量 | `Wait` / `Reduce` | ★★★★☆ |
| R2: 缩量加速是卖点 | **ExhaustionMove** | `price_accel_3d>3% && volume_ratio<0.85` | `Wait` | ★★★★★ |
| R4: 爆量换手才是顶 | **BlowoffTop** | 5日涨后首阴+放量≥2倍+`close_position<0.3` | `Reduce` | ★★★★★ |
| 健康回调靠近 MA20 | **HealthyPullback** | Buy信号+回调+`close_position>0.6`+MA20附近 | `BuyNow` | ★★★★★ |
| 尾盘走弱 | **WeakClose** | `close_position<0.3` | `Wait` | ★★★★☆ |

**扩展标准**：当 Pattern 数量 ≥ 8 时，再拆 `patterns/` 目录。

---

#### 实施计划

**Phase1（1 周）** — 极简过滤器
- 初始化 `crates/execution-engine/`
- `types.rs`: 5 State + ReasonTag + SkipReason + IntradaySnapshot
- `fetcher.rs`: Tencent API 批量快照（失败 → Skip）
- `engine.rs`: 3 Pattern 函数 + 主编排
- CLI: `preclose-analysis` 命令
- 输出: stdout / JSON

**Phase2（90 天）** — 样本积累
- 每日运行 `preclose-analysis`
- 输出到 `reports/execution-samples/YYYY-MM-DD.json`（含 snapshot）
- 记录: symbol, signal, execution_state, reasons, snapshot
- 禁止: 新增 Pattern, 调参数, Backtest v2, 性能声称

**Phase3（90 天后）** — 决定是否扩展
- 分析 Execution Sample Library
- 决定是否: 新增 Pattern, 拆 `patterns/` 目录, Backtest v2, 前端面板

---

#### 验证状态

- 编译: `cargo check` 全 workspace 通过
- 测试: 相关测试通过
- CLI: `cargo run -p quant-cli -- preclose-analysis --scope cn` 正常输出
- 无 B类禁止项触碰

#### MemGuard 提交

- **ADR-070**: Execution Layer Governance Boundary — 物理隔离，单向消费，不修改冻结层
- **ADR-071**: Execution Layer 90-Day Observational Ban — 90 天禁止性能声称、禁止优化、禁止 Backtest v2
- **ADR-072**: Execution Layer Evolves into a Pattern Library — 扩展 ADR-070，确认演化方向
- **TASK-120**: Execution Layer Foundation — Phase1 极简过滤器，3 Pattern + 5 State + CLI only + 纯内存

---

### [待追加]

### 2026-06-25 — 前端 UI 精细化：SignalsPanel 悬浮详情卡片

本轮优化属于 **A类（基础设施/可观测性）**，不触碰任何 B类禁止项。目标：提升信号归因信息的可达性与阅读体验，将原本需要点击弹窗的详情改为悬浮即时查看。

#### 完成内容

| 阶段 | 目标 | 结果 | 验证 |
|------|------|------|------|
| **Phase 1** | 嵌入 SignalDetailModal 视觉设计 | 完整视觉设计（header + 堆叠条 + 4 维度卡片 + 公式）嵌入每个信号卡片 | 构建通过 |
| **Phase 2** | 改为 2×2 网格布局 | 四维度卡片从竖排 1×4 改为网格 2×2 | 构建通过 |
| **Phase 3** | 卡片扩宽 + 字号增大 | 宽度从 26rem → 46rem，所有字号 +2 | 构建通过 |
| **Phase 4** | 强制 nowrap 文本 | 所有文本元素 `white-space: nowrap` + `flex-shrink: 0` | 构建通过 |

#### 设计范式更新

本轮沉淀了两条新的前端编码范式（已录入 `knowledge/patterns.md`）：

- **CSS Hover 内嵌详情卡片**：复杂数据详情优先使用纯 CSS hover 触发内嵌卡片，替代独立弹窗/抽屉/滑出面板
- **强制 nowrap 文本策略**：悬浮卡片、内嵌详情面板等空间有限场景，所有文本元素必须声明 `white-space: nowrap`，防止 flex/grid 容器内因换行导致布局崩坏

#### 技术细节

- **触发方式**：纯 CSS hover（`.signal-card:hover .signal-detail-card`），无点击、无弹窗、无 JavaScript 状态管理
- **布局**：`grid-template-columns: 1fr 1fr`（2×2），`width: 46rem`
- **字号**：header 1.4rem → 2.2rem，卡片内 0.9rem → 1.15rem
- **文本策略**：`white-space: nowrap` + `flex-shrink: 0` 全覆盖

#### 验证状态

- 构建：`npm run build` 通过（794ms，0 错误）
- 推送：已推送至 `origin/v5`

#### MemGuard 提交

- **TASK-092** → Done（symbol-diagnostics CLI 已验证，全部字段输出正确）
- **TASK-093** → In Progress（90 天 divergence 观察已启动，每日收集样本）

---

### 2026-06-25 — MemGuard 任务状态审计与知识库同步

本轮属于 **治理清洁（Governance Hygiene）**，不触碰任何代码或行为。

#### 审计范围

对已完成的 080 系列任务、TASK-083、TASK-120、TASK-123 进行状态核对，确认是否应标记为 Done。

#### 审计结果

| 任务 | 状态变更 | 依据 |
|------|----------|------|
| TASK-080A | **Phantom Task**（从未创建） | 仅在设计阶段存在，未进入 MemGuard 追踪系统 |
| TASK-080B | Todo → **Done** | 已完成 4-table 分析（Pearson, Spearman, MI, Predictive Orthogonality） |
| TASK-080C | Todo → **Done** | 已完成 4 现有因子 + 6 缺失因子实证分析 |
| TASK-080D | Todo → **Done** | 已完成 K-means 聚类，推荐 3-State（variance ratio 0.862） |
| TASK-080E | Todo → **Done** | 已识别 Fed Funds 原始水平为 regime 标识符，Z-score 为正确指标 |
| TASK-080F | Todo → **Done** | 已落地 252d Z-score + ±3 capping，CN IG 0.474→0.964 |
| TASK-083 | Todo → **Done** | Universe 已从 18 扩展至 25 symbols，回填 23,000 行 |
| TASK-120 | 新建 → **Done** | Execution Layer Phase 1 已交付、测试、上线 |
| TASK-123 | Todo → **Done** | Research Layer Refactor 完成（41 files, 795+/4681-） |
| TASK-092 | Todo → **Done** | 验收标准逐项验证：Final Score + 4 Contributions + Raw Scores + Rotation Rank 全部存在 |
| TASK-093 | Todo → **In Progress** | 前置条件（092）已满足，90 天观察已启动 |
| ADR-057 (TASK-007) | Todo → **Superseded** | 已废弃，HK 流动性主导模型无需（ADR-059 修复锚点即可） |

#### 关键发现：Phantom Task

**080A 不存在于 MemGuard 中**。这是一个设计阶段编号，被文档引用但从未创建为追踪任务。与 ADR-060（Wave 9）共享了 TASK-010 编号，导致混淆。

**处理方式**：
- 不创建 080A 为假任务
- 不标记为 Done
- 记录为 `080A = Historical Phase (Not Tracked By MemGuard)`
- 未来彻底忽略该编号

#### 关键原则

审计中纠正了三个常见误判：
1. `ADR Accepted ≠ Task Done`
2. `README 存在命令 ≠ 功能完成`
3. `观察期运行 ≠ 未完成`

验收标准必须是：**实际运行 + 字段逐项核对**，而非间接证据推断。

#### 知识库同步

- `knowledge/patterns.md`：新增 CSS Hover 内嵌详情卡片 + nowrap 文本策略
- `memory/context.md`：Phase 更新为 `v4.5_released_validation_sprint`
- `memory/tasks_archive.md`：归档已完成任务

---

### 2026-06-17 — Shadow Production v1 正式启动

根据复核结论，项目里程碑已从**"量化策略 v1 完成"**转变为**"研究治理体系 v1 完成"**。核心成果是建立了完整的证据链（Provenance Chain）而非单纯优化回测指标。

#### Shadow Production v1 规则

**Research Program: CLOSED (2026-06-17)**
**Shadow Production v1: OPEN (2026-06-17)**

##### A类：允许（基础设施）
以下开发不会改变模型行为，仅增强可观测性和运维能力：
- ✅ Monitoring（监控）
- ✅ Dashboard（仪表板）
- ✅ Alerting（告警）
- ✅ Observability（可观测性）
- ✅ Provenance（可追溯性：如 `git_commit` / `generated_at` 注入）
- ✅ Reporting（报告）
- ✅ Automation（自动化）
- ✅ 文档和 ADR 更新

##### B类：禁止（行为改变）
以下任何改动都会污染基线、破坏 Shadow Production 的有效性：
- ❌ 新因子（New Factor）
- ❌ 新权重（New Weight）
- ❌ 新阈值（New Threshold）
- ❌ 新状态机（New State Machine）
- ❌ 新分类逻辑（New Classification Logic）
- ❌ 新 Taxonomy（New Taxonomy）
- ❌ 调参数（Threshold Tuning）
- ❌ 调权重（Weight Tuning）
- ❌ 调因子（Factor Tuning）
- ❌ 状态机变化（State Machine Changes）
- ❌ 经济分类法变化（Economic Taxonomy Changes）
- ❌ 回测执行语义变化（Backtest Execution Semantics）

**禁止期限**：90 天（至 2026-09-17）
**唯一例外**：触发 Kill Criteria（见下文）

#### 90 天观察计划

目标不是**证明系统正确**，而是**寻找系统失效的证据**。

| 阶段 | 天数 | 观察重点 | 关键指标 |
|------|------|----------|----------|
| **Phase A** | 1-30 | State Layer | 状态稳定性、切换频率、NO_TRADE% |
| **Phase B** | 31-60 | Economic Layer | 3-State 分布漂移、Forward Return 对齐、PSI |
| **Phase C** | 61-90 | Allocation Layer | 仓位建议 vs 回测、换手率、Drawdown 跟踪 |

#### Kill Criteria（终止条件）

若触发以下任一条件，可打破 90 天锁并启动新的 ADR 流程：

| 类别 | 代码 | 描述 |
|------|------|------|
| **State** | S1 | State Layer 不稳定：RiskOn/RiskOff/Neutral 切换频率超过历史 3σ |
| **State** | S2 | 状态持久性崩溃：NO_TRADE 或 DE_RISK 连续超过 30% 天数，无宏观理由 |
| **State** | S3 | 状态信号矛盾：State 推荐 PROCEED，但 100% 信号为 Hold/Watch 超过 5 天 |
| **Economic** | E1 | 经济层错位：Forward Return 分布违反 ADR-063 方差比率（<0.6） |
| **Economic** | E2 | 制度宏观 disconnect：Favorable 经济状态但 RiskOff 市场制度超过 10 天 |
| **Economic** | E3 | 经济因子退化：>2 核心因子永久不可用或出现结构性断裂 |
| **Allocation** | A1 | 配置穿透：仓位 sizing 违反状态推荐超过 20%（回测中） |
| **Allocation** | A2 | 回撤 breach：实盘回撤超过回测 MaxDD 的 1.5 倍（同窗口） |
| **Allocation** | A3 | 策略漂移：Best strategy 从 MomentumRight 变为 ValueLeft 超过 30 天，无宏观转变 |
| **Data** | D1 | 数据管道失败：>2 连续天缺失市场数据，>50% 标的 |
| **Data** | D2 | 提供商结构性变更：Eastmoney/Tencent API 永久变更，破坏 ingestion |

#### 当前冻结基线

| 组件 | 冻结版本 | 基线运行 ID |
|------|----------|-------------|
| State Layer | v1.0 | ADR-061/062/063 |
| Backtest Engine | v1 | `bt-20260617090905` |
| 可追溯链 | v1 | `run_version=v1`, `git_commit=df9904e...`, `generated_at=2026-06-17 09:09:05 UTC` |
| 数据 | 2026-06-16 | 所有管道阶段完成 |

#### 后续开发分类

所有未来开发必须明确标记为 **A类（基础设施）** 或 **B类（行为改变）**。B类 在 90 天内禁止，除非触发 Kill Criteria。

---

### 2026-06-17 — 复核结论与项目治理原则

#### 核心原则
> **Do not optimize on explanations. Optimize only on evidence.**
>
> （不要因为解释听起来合理而优化，只因为证据证明需要优化才优化。）

#### 项目成熟度评估
| 模块 | 成熟度 | 状态 |
|------|--------|------|
| 数据链路 | 8/10 | 稳定 |
| 回测语义 | 8.5/10 | 已修复（DeRisk=30%） |
| 状态机契约 | 9/10 | 冻结（ADR-061/062/063） |
| Economic Layer | 7/10 | Beta（3-State Taxonomy 冻结） |
| Provenance | 9/10 | 完整（run_version + git_commit + generated_at） |
| Shadow Production | 已启动 | 90 天观察期 |

#### 当前最大风险
已经不是代码错误，而是：
> **人为过度优化（Optimization Trap）**

说白了：现在最大的 Bug 已经不是 Rust 代码，而是研究者本人。

#### TASK-091 评估结论
- **处理方式**：建立观测（Signal-State Divergence）而非修改逻辑
- **性质**：A类（基础设施/可观测性），不触碰 B类禁止项
- **价值**：为 Shadow Production 提供可追踪的样本库（StrongBuy × DeRisk 组合）
- **后续**：90 天后统计 T+20/T+60/T+120 收益分布，回答该组合是机会还是风险

#### Phase B 预留观察项
- Risk Score 拆解（Volatility/Macro/Breadth Stress/Credit/Liquidity 贡献）
- 仅用于理解，**不产生任何动作**（不调权重、不调阈值、不修改状态机）
- 任何优化必须等待 90 天观察期结束或 Kill Criteria 触发

---

## 附录

### A. 与 V4 演进路线的衔接

```
V4: Research Cognition Layer
  └─ Structured Output + Skill System + Provider Abstraction + Research Context
     └─ 先做少做深：1 个完整链路打磨稳定

V5: Research Memory（当前规划）
  └─ Regime transition history + Analysis memory + Skill evolution tracking

V6: Multi-Agent（未规划）
  └─ Macro Agent + China Agent + Risk Agent + Orchestrator

V7: Research OS（未规划）
  └─ Marketplace + Third-party skills + Consensus Engine
```

### B. 相关文档索引

- `设计规划-v4.md` — V4 完整设计规划（含 Wave 1-6 实施路线图）
- `docs/阶段性更新.md#2026-06-02` — V4 桌面端 LLM 集成落地成果
- `docs/文档状态说明.md` — 文档状态与读取顺序指南
- `memory/decisions.md` — ADR 决策记录（ADR-048 ~ ADR-063）
- `memory/context.md` — 当前任务状态（TASK-004 ~ TASK-081）

### 2026-06-18 — 代码级结构优化（A类：基础设施）

本轮优化属于 **A类（基础设施）**，不触碰任何 B类禁止项。

#### 完成内容

| 阶段 | 目标 | 结果 | 验证 |
|------|------|------|------|
| **Phase A** | 清理编译器 warning | 9 个 warning 全部消除 | `cargo check` 全 workspace 通过 |
| **Phase B** | 提取重复 `apply_persistence` | 6 个副本 → `regime-audit/src/common.rs` | 编译+测试通过 |
| **Phase C** | 拆分 `market-store` | 2,678 行 → 154 行 lib.rs + 14 域模块 | 编译+测试通过 |
| **Phase D** | 拆分 `app-service` | 5,258 行 → 4,083 行 lib.rs + 6 helper 模块 | 编译+测试通过 |
| **Phase E** | 拆分 `cli` main.rs | 4,838 行 → 712 行 main.rs + 9 命令模块 | 编译+测试通过 |

#### 关键决策

- `p50` 字段保留（完整百分位分布，添加 `#[allow(dead_code)]` + 注释）
- `p1` 变量保留（Wave 8 实验设计，前缀改为 `_p1`）
- `classify_raw_regime` 保留重复（1 个语义变体："riskon" vs "risk_on"）
- `apply_persistence` 提取到 `common.rs`（6 个字节级相同副本）
- AppContext impl 块不拆分（Oracle 建议：高耦合边界，保持完整）
- CLI match 分发 O(1) 零开销，无需宏抽象

#### 验证状态

- **编译**：`cargo check` 全 workspace 通过，耗时 3.08s
- **测试**：68/68 全部通过
- **CLI 状态**：`cargo run -p quant-cli -- status` 正常输出 JSON
- **Oracle 复核**：通过，评级 LOW RISK

#### 文档更新

- `docs/功能模块与处理逻辑.md`：更新 `market-store` 风险点为"已解决"，更新 `app-service` LLM 方法描述
- `docs/系统架构与数据流.md`：更新 `market-store` 和 `app-service` 架构描述，添加模块拆分说明
- `crates/regime-audit/AGENTS.md`、`crates/AGENTS.md`、根 `AGENTS.md`：更新工程目录结构与任务状态

#### MemGuard 提交

- **TASK-082**：Done（代码级调优：warning 清理、重复消除、模块拆分）

---

### 2026-06-18 — README 文档重组（A类：基础设施）

本轮优化属于 **A类（基础设施）**，不触碰任何 B类禁止项。Oracle 复核通过，评级 **WARN → 修复后确认**。

#### 问题诊断

Oracle 发现 README 中 CLI 命令存在严重冗余：
- 6 个 pipeline 命令在 section 8（详细说明）和 section 13（快速参考）中重复出现
- `dashboard-snapshot` 5 个变体、 `export-report` 3 个变体重复列示
- 40 个审计/验证命令完全无文档（隐藏知识）
- 2 个已知限制（`market-store` / `app-service` 偏大）已过时

#### 决策与方案

**编号表（已确认）：**

| 新编号 | 章节 | 来源 | 说明 |
|--------|------|------|------|
| 1–8 | 不变 | 原 1–8 | 保持 |
| **9** | **检查与排查** | 原 8.11 诊断命令 + 原 9 `check-data-health` | 合并诊断类 |
| 10 | 桌面端启动方法 | 原 10 | 不变 |
| 11 | 推荐使用流程 | 原 11 | 含"高级参考：手动分步执行"子节 |
| **12** | **查看与导出** | 原 8.11 查询 + 原 8.12 导出 + 原 9 `export-data-health-report` | 合并查询导出类 |
| **13** | **LLM 智能分析** | 原 8.8 | 独立章节 |
| **14** | **回测** | 原 8.10 | 含完整参数说明 |
| **15** | **诊断与调试** | 新增 | `status` + 40 个审计命令，标注"研究/实验用途" |
| 附录 A | 已知限制 | 原 12 | 更新描述 |

**40 个审计命令分组（已确认）：**

| 分组 | 标题 | 数量 |
|------|------|------|
| Ground Truth | 真值审计与标签生成 | ~15 |
| 观察层/归因 | 观察层与归因审计 | ~10 |
| 状态/信号分解 | 状态与信号分解 | ~5 |
| 经济/配置实验 | 经济与配置原型 | ~5 |
| 前沿/机制 | 前沿与机制实验 | ~5 |

统一标注："以下为研究/实验命令，输出格式可能随版本变更，不保证向后兼容。"

**关键修复：**
- `dashboard-dates` 放入新 12（查看与导出）
- `status` 放入新 15（诊断与调试）
- `run-backtest` 补全 `--scope` / `--use-state-sizing` / `--max-drawdown` 参数
- `app-service` 限制改为："已模块化（lib.rs + 7 个辅助模块），但 lib.rs 仍较为庞大，后续可进一步拆分"
- `market-store` 限制删除（已完全解决）
- 原 section 11 的 7 条 gate/latest 语义注释按主题拆分到新 9/11/12 对应步骤中

#### 实施结果

- `README.md` 从 578 行 → 588 行，结构重组完成
- 消除了 section 8 与 section 13 之间的命令重复
- 40 个审计命令首次获得文档覆盖
- 编号无冲突，逻辑流：日常流程 → 查看导出 → LLM 分析 → 回测 → 诊断调试

#### 验证状态
---

### 2026-06-23 — Research Layer 重构：从 Skill Framework 到 Prompt-Only Narrative Layer

本轮重构属于 **A类（基础设施/可观测性）**，不触碰任何 B类禁止项。目标：将 LLM 集成从「第二套量化系统」收敛回「冻结引擎之上的只读叙事层」。

#### 背景：为什么必须重构

V4 的 LLM 集成（`research-skills` crate）已经自然演化出一套复杂的 Skill/Agent/YAML 框架：
- 6 个 Skill（YAML front matter + trigger + schema）
- Agent Profile（YAML 配置：priority、reasoning_style、risk_tolerance、confidence_model）
- Skill Router（触发评估 + 权重排序）
- Reasoning Graph（执行流编排）
- 前端：6 标签页面板（Conclusion / Regime / Execution / Risk / History / Router）+ Agent 选择器 + Skill 下拉 + 比较分析 + 配置编辑器

这本质上是在冻结量化引擎之上重建了第二套评分系统。GPT 审查指出：
> 「保留知识，删除框架。Skill 又会慢慢演化成另一套规则系统。」

#### 核心调整：彻底收敛

**调整 1：从 3 个 Agent → 1 个研究员**

不需要 Agent Framework。不是 3 个 Agent，而是 1 个研究员 + 5 种提问方式（Prompt Variant）：

```
[市场叙事]      →  prompt: "今天市场发生了什么？"
[为什么没交易]   →  prompt: "系统内部哪里存在矛盾？"
[收盘前复核]    →  prompt: "Execution 为什么这样做？"
[风险视角]     →  prompt: "如果我是风控总监，我最担心什么？"
[唱反调]      →  prompt: "系统可能错在哪里？"
```

本质上就是 `ResearchAction` enum + `build_prompt()`，没有 Agent 对象、没有 YAML 配置、没有权重。

**调整 2：从 5 个 Skill → 0 个 Skill，保留知识为 Prompt Fragments**

删除 Skill Framework（registry、router、trigger、schema、YAML）。但**不删除旧知识**：
- `factor-composite` / `liquidity-shock` / `sector-rotation` / `macro-linkage` / `volatility-tail` 的 SKILL.md 文本内容保留为 **Prompt Fragments**
- 在 `build_prompt()` 中按需拼接 liquidity fragment、macro fragment、volatility fragment
- 没有 `invoke_skill()`，没有 `evaluate_triggers()`，没有 `weight`

**调整 3：前端从复杂面板 → Insight Panel（5 个按钮）**

不暴露：
- ❌ Agent Selector
- ❌ Skill Dropdown
- ❌ Output Format Tabs
- ❌ Config Editor
- ❌ Comparison Mode
- ❌ History Browser
- ❌ Token Usage Display
- ❌ Confidence Score
- ❌ Triggered / Not Triggered Badge

用户只看到：
```
┌─────────────────────────────┐
│ Insight Panel              │
│ ○ Market Story              │
│ ○ Why NoTrade?              │
│ ○ Preclose Review           │
│ ○ Risk View                 │
│ ○ Devil's Advocate          │
│ [Generate]                  │
├─────────────────────────────┤
│                             │
│ Markdown 输出区域            │
│                             │
└─────────────────────────────┘
```

像 ChatGPT。点击按钮 → 生成 Markdown → 渲染展示。结束。

**调整 4：输出格式——保留事实，禁止评分**

允许：
```markdown
- 连续上涨 5 天
- 距离 MA20 +7%
- 尾盘成交量占全天 38%
```

禁止：
```json
confidence = 0.83
fit_score = 0.75
probability = 62%
ranking = #1
recommendation = BUY
```

输出就是 Markdown 叙事。没有 JSON schema。没有结构化字段。

**调整 5：架构定位**

```
Frozen Quant Engine
  ├─ State
  ├─ Signal
  ├─ Rotation
  ├─ Execution (观察期)
  └─ ...
         ↓
  Research Layer (LLM) ← 只读
         ↓
  Markdown Narrative
         ↓
  Human
```

> Research Layer is a read-only narrative layer above the frozen quantitative engine. It explains, questions, and contextualizes decisions, but never creates, ranks, or modifies them.

#### 新的核心类型

```rust
// crates/research-skills/src/action.rs (或直接在 lib.rs 中)
pub enum ResearchAction {
    MarketStory,
    ContradictionReview,
    PrecloseReview,
    RiskView,
    DevilsAdvocate,
}

pub fn build_prompt(action: ResearchAction, snapshot: &DashboardSnapshot) -> String {
    let base = build_base_context(snapshot);
    let fragment = match action {
        ResearchAction::MarketStory => MARKET_STORY_PROMPT,
        ResearchAction::ContradictionReview => CONTRADICTION_REVIEW_PROMPT,
        ResearchAction::PrecloseReview => PRECLOSE_REVIEW_PROMPT,
        ResearchAction::RiskView => RISK_VIEW_PROMPT,
        ResearchAction::DevilsAdvocate => DEVILS_ADVOCATE_PROMPT,
    };
    format!("{}\n\n{}", base, fragment)
}

// 旧 skill 知识保留为 prompt fragments
const LIQUIDITY_FRAGMENT: &str = include_str!("../fragments/liquidity.md");
const MACRO_FRAGMENT: &str = include_str!("../fragments/macro.md");
const VOLATILITY_FRAGMENT: &str = include_str!("../fragments/volatility.md");
```

#### 删除清单

| 文件/目录 | 原因 |
|-----------|------|
| `research-skills/skills/factor-composite/` | Skill Framework 已删除，知识提取到 fragment |
| `research-skills/skills/liquidity-shock/` | 同上 |
| `research-skills/skills/sector-rotation/` | 同上 |
| `research-skills/skills/macro-linkage/` | 同上 |
| `research-skills/skills/volatility-tail/` | 同上 |
| `research-skills/src/agent_profile.rs` | Agent Framework 已删除 |
| `research-skills/src/trigger.rs` | Skill trigger 已删除 |
| `research-skills/src/router.rs` | Skill router 已删除 |
| `research-skills/src/registry.rs` | Skill registry 已删除 |
| `research-skills/src/schema.rs` | Output schema 已删除 |
| `research-skills/src/reasoning.rs` | Reasoning graph 已删除 |
| `research-skills/src/executor.rs` | Skill executor 已删除 |
| `research-skills/src/token_budget.rs` | Token budget 保留（成本控制） |
| `frontend/src/components/AgentProfileEditor.vue` | Agent 配置编辑器已删除 |
| `frontend/src/components/SkillRouterPanel.vue` | Skill 路由面板已删除 |
| `frontend/src/components/SkillBadge.vue` | Skill 徽章已删除 |
| `frontend/src/components/InsightPanel.vue` | 重写为纯 Markdown 展示（无 confidence/recommendations） |
| `frontend/src/components/LlmAnalysisPanel.vue` | 重写为 InsightPanel（5 按钮 + Markdown 输出） |

**保留**：
- `research-skills/src/provider.rs` — LLM Provider trait
- `research-skills/src/openai_provider.rs` — OpenAI 调用实现
- `research-skills/src/deterministic.rs` — 测试模式
- `research-skills/src/analysis.rs` — ResearchAnalysis 类型（简化）
- `research-skills/src/renderer.rs` — Markdown 渲染器（简化）

#### 前端新结构

```
frontend/src/components/ResearchInsightPanel.vue (新)
├── 5 个 action 按钮（MarketStory / WhyNoTrade / PrecloseReview / RiskView / DevilsAdvocate）
├── Markdown 输出区域（v-html 渲染）
└── 加载状态
```

#### 实施路径（Phase 1，预计 1 天）

**Step 1：删除旧 Skill 目录和文件**
- 删除 5 个 `skills/` 子目录
- 提取 YAML 中的描述文本保存到 `fragments/` 作为纯 Markdown（不作为代码引用）
- 删除 `agent_profile.rs`, `trigger.rs`, `router.rs`, `registry.rs`, `schema.rs`, `reasoning.rs`, `executor.rs`

**Step 2：创建 ResearchAction**
- 在 `research-skills/src/` 新增 `action.rs`
- 定义 `ResearchAction` enum + `build_prompt()` 函数
- 5 个 Prompt 直接写为 `const &str`

**Step 3：简化 research-skills crate**
- 更新 `lib.rs` 模块声明
- 删除 `AgentProfile`, `RiskTolerance`, `OutputDepth`, `OutputFormat` 等类型
- 保留 `LlmProvider`, `OpenAiProvider`, `InferenceConfig`

**Step 4：更新 app-service 分析入口**
- 将 `analyze_with_skill(agent, skill)` 改为 `analyze_with_action(action)`
- 删除 Agent 选择和 Skill 路由逻辑

**Step 5：前端重写**
- 删除 `LlmAnalysisPanel.vue`, `SkillRouterPanel.vue`, `AgentProfileEditor.vue`, `SkillBadge.vue`
- 重写 `InsightPanel.vue` 为纯 Markdown 展示面板
- 新增 `ResearchInsightPanel.vue`（5 按钮 + 生成）
- 更新 `store.js`：删除 `selectedAgent`, `compareAgent`, `llmHistory`, `showAgentEditor` 等状态
- 更新 `tauri.js`：删除 `listAgentProfiles`, `listSkills`, `evaluateSkillTriggers`, `saveAgentProfile`, `readAgentProfile` 等命令

**Step 6：Tauri 命令清理**
- 删除 `src-tauri/src/lib.rs` 中相关命令注册
- 保留 `analyze_with_llm`（但改为接受 `action` 参数而非 `agent`+`skill`）

**Step 7：i18n 清理**
- 删除 `locales/zh.json` 和 `locales/en.json` 中大量 LLM 相关键
- 新增 5 个 action 按钮的翻译键

#### Phase 2（90 天后）

- **Postmortem**：`Why was I wrong?` 按钮（输入最近 signal/state/execution + actual return，输出复盘叙事）
- **Historian**：`以前出现过类似情况吗？`（基于历史样本的叙事比较）
- 仍不做：weekly-research、monthly-retrospective（避免 Backtest v2 / Optimization Trap）

#### MemGuard 提交计划

- **ADR-074**: Research Layer Governance Boundary — 只读叙事层，永不创建/评分/排序/修改决策
- **TASK-123**: Research Layer Refactor — 删除 Skill Framework，收敛为 5 Action + Markdown 输出

---

## 附录 B. 与 V4 演进路线的衔接（更新）

```
V4: Research Cognition Layer
  └─ Structured Output + Skill System + Provider Abstraction + Research Context
     └─ 先做少做深：1 个完整链路打磨稳定

V4.5: Research Layer Refactor (当前)
  └─ 删除 Skill Framework，收敛为 Prompt-Only Narrative Layer
  └─ 5 个 Research Action + Markdown 输出
  └─ 前端 Insight Panel（ChatGPT 风格）

V5: Research Memory
  └─ Regime transition history + Analysis memory + Prompt evolution tracking

V6: Multi-Agent（冻结，不规划）

V7: Research OS（冻结，不规划）
```

**关键区别**：V4.5 不是新增功能，而是**删除框架**。从「LLM Engine」回退到「Research Layer」。

