# 数据源方案评审（中文）

> **文档状态说明**：这是一份阶段性数据源评审文档，主要记录当时为什么选择 Eastmoney / Tencent / FRED 作为当前主链路。
>
> **归档级别**：历史评审参考。
>
> 它适合用于理解“为什么当时不直接采用 Yahoo / Tushare 作为默认主源”，但**不应替代当前实现事实**。
>
> 当前运行时事实与主参考请优先看：
>
> - `README.md`
> - `docs/系统架构与数据流.md`
> - `docs/功能模块与处理逻辑.md`
> - `docs/文档状态说明.md`

## 1. 结论先说

不建议直接照搬你原始的 **方案 A：A 股用 Tushare、港股用 Yahoo Finance**。

原因不是思路错，而是它在当前项目和当前运行环境下，**可靠性并不对称**：

- **Tushare 做 A 股日线**：合理，但需要 token / 积分，且更适合作为后续增强源
- **Yahoo 做港股日线**：在当前环境里已直接实测到 `403`，不适合做默认主源

因此，当前项目应采用一个**修正版低成本方案**。

---

## 2. 当前项目建议采用的低成本方案（修正版 A）

### 运行时默认方案

- **A 股指数 / ETF**：Eastmoney 主源，Tencent 兜底
- **港股指数**：Eastmoney 主源，Tencent 补充 / 兜底
- **宏观因子**：FRED
- **日线口径**：统一采用 **前复权 / qfq** 逻辑，避免趋势指标跨源口径漂移

### 可选增强方案

- **Tushare**：后续可接为 A 股增强源
  - 适合：更规范字段、更稳定历史拉取、长期本地数据库建设
  - 代价：需要 token / 积分，不是零门槛
- **Yahoo Finance**：只建议保留为“可选备选源 / 手动核验源”
  - 不建议作为港股默认主源

---

## 3. 为什么不直接采用 Yahoo 作为港股默认主源

### 当前环境实测结果

我已经直接验证过港股历史接口：

- `Yahoo Finance`：返回 **HTTP 403**
- `Eastmoney`：港股指数历史数据可正常返回
- `Tencent`：恒生科技、恒生国企等核心港股指数可正常返回

### 工程含义

如果项目目标是“个人本地研究工具”，那数据源不需要完美，但至少需要：

1. **可以稳定抓到历史日线**
2. **接口不经常失效**
3. **不用为了一个默认源不断修适配器**

从这个角度看，Yahoo 更适合作为：

- 低频手动校验
- 备份 / 参考来源

而不适合作为默认主链路。

---

## 4. 为什么当前项目继续沿用 Eastmoney + Tencent 更合理

因为这套组合已经在当前项目里被验证过：

- 已完成 `seed-universe`
- 已完成 `ingest-daily`
- 已完成 `indicator / macro / rotation / strategy / signal / backtest / report / desktop`

也就是说，**这不是纸面建议，而是已经跑通的生产前链路**。

在这种情况下，最稳妥的做法不是为了迎合理论上的“更漂亮方案”去重写 provider，而是：

- 保持当前可运行主链
- 扩大 universe
- 补齐 symbol metadata
- 把 Tushare 留作后续增强选项

---

## 5. 当前已落地的 universe 扩展原则

本次已按你提供的指数 / ETF 范围扩展到以下方向：

### 指数

- 上证指数
- 上证50
- 沪深300
- 科创50
- 科创100
- 中证1000
- 中证500
- 创业板指
- 创业板50
- 恒生国企指数
- AH 股溢价指数
- 恒生科技指数

### ETF

- 沪深300ETF
- 创业板ETF
- 恒生科技ETF
- 银行ETF
- 券商ETF
- 有色ETF
- 通信ETF
- 电力ETF
- 人工智能ETF
- 金融科技ETF

---

## 6. `display_symbol` 的中文解释

你给的 `display_symbol` 很适合保留，但它**不应该替代真实 provider symbol**。

建议这样理解：

- `symbol`：系统内部主标识，例如 `000300`、`HSTECH`
- `display_symbol`：展示 / 人类可读 / 延续旧系统风格的显示标识，例如 `1A0300`、`HK_HSTECH`、`E510300`
- `eastmoney_secid`：Eastmoney 实际抓数标识
- `tencent_symbol`：Tencent 实际抓数标识

也就是说：

- `display_symbol` 是**展示层元数据**
- `eastmoney_secid / tencent_symbol` 是**抓数层元数据**

这两层不要混用。

---

## 7. 中文翻译补充（便于后续文档统一）

### Scheme A（最低成本）

- **A股**：Tushare
- **港股**：Yahoo Finance

### 优点

- 免费 / 低成本
- 接入门槛低

### 缺点

- 港股默认依赖 Yahoo，稳定性不够理想
- Yahoo 在当前环境里已出现 403
- Tushare 虽然合理，但不是零门槛，需要 token / 积分

### 修正版方案 A（推荐落地）

- **A股**：Eastmoney 主源，Tencent 兜底
- **港股**：Eastmoney / Tencent 低成本组合
- **Tushare**：后续增强源
- **Yahoo Finance**：仅备选，不做默认主源

---

## 8. 最终建议

如果目标是：

- **现在就能跑**
- **最低成本**
- **尽量可靠**

那么当前项目最合理的选择是：

> **保留 Eastmoney + Tencent 作为默认运行时数据源，扩展 universe 与 symbol metadata，并把 Tushare 作为后续可选增强源。**

这比“直接把 Yahoo 设成港股默认主源”更符合当前项目的实际情况。

补充：

- 当前代码已统一为：**Eastmoney `fqt=1` + Tencent `qfq`**
- 这更适合 `MA20 / MA60 / MACD` 这类长线趋势分析
