# 系统架构与数据流

本文档用于梳理当前项目的**整体架构**、**数据来源**、**数据流转路径**与**关键日期语义**。

---

## 1. 系统定位

`rust-quant-analysis-system` 是一个面向 **指数 / ETF、低频、趋势、长线** 的本地桌面量化研究系统。

当前系统目标不是做：

- 高频执行
- 自动下单
- 多用户在线服务

当前系统目标是：

- 采集日线数据
- 生成技术指标
- 结合宏观环境、轮动强弱、策略偏好生成信号
- 做基础回测
- 导出报告
- 在桌面端展示结果

---

## 2. 总体架构

```text
外部数据源
  ├─ Eastmoney（CN 指数 / ETF、HK 指数部分）
  ├─ Tencent（HK 指数与部分 fallback）
  └─ FRED（宏观因子）

        ↓

crates/data-ingestion
  ├─ universe 加载
  ├─ provider fetch
  ├─ fallback 逻辑
  ├─ freshness-aware provider fallback
  └─ 日线标准化（前复权口径）

        ↓

crates/market-store
  ├─ ClickHouse 写入/读取
  └─ SQLite 本地状态
  ├─ 按域拆分为 14 个模块：core, bars, signals, regime, environment,
  │  rotation, strategy, indicators, macro, backtest, reports, dates,
  │  instruments, sqlite（通过 `pub use` 重新导出）
  └─ lib.rs 保持 154 行 thin wrapper
  ├─ 按域拆分为 14 个模块：core, bars, signals, regime, environment,
  │  rotation, strategy, indicators, macro, backtest, reports, dates,
  │  instruments, sqlite（通过 `pub use` 重新导出）
  └─ lib.rs 保持 154 行 thin wrapper

        ↓

计算引擎层（crates/*-engine）
  ├─ indicator-engine
  ├─ macro-engine
  ├─ rotation-engine
  ├─ strategy-engine
  ├─ signal-engine
  ├─ backtest-engine
  └─ report-engine

        ↓

crates/app-service
  ├─ 串联整条分析链
  ├─ 提供 CLI 能力
  ├─ 提供 Tauri 命令能力
  ├─ 负责报告/健康检查/刷新编排
  ├─ lib.rs 保留 AppContext 高层编排（4,083 行）
  └─ 6 个 helper 模块提取（llm.rs, llm_analysis.rs, llm_config.rs, refresh.rs, report.rs, health.rs）

        ↓

应用层
  ├─ apps/cli
  └─ apps/desktop (Tauri + frontend)
```

---

## 3. 数据来源

### 3.1 行情数据

#### CN 指数 / ETF

- **主源**：Eastmoney
- **兜底**：Tencent

#### HK 指数

- **主源 / 组合源**：Eastmoney + Tencent

#### 当前不作为默认主源

- Yahoo Finance
- Tushare

说明：

- 当前运行环境里，Yahoo Finance 在历史接口上出现过 `403`
- Tushare 没有作为当前 V1 默认依赖
- universe 现在区分：
  - `enabled=true`：仍属于研究 universe
  - `latest_gate_required=true/false`：是否参与“最新 dashboard/report 可用日期”的完整性门槛
- 这意味着某些已知 provider 不稳定但仍有研究价值的标的（例如部分跨市场连接类指数）可以继续保留在 universe 中，但不再永久阻塞 GLOBAL/HK 最新报告

### 3.2 宏观数据

- 来源：**FRED**
- 当前使用的核心因子：
  - `VIXCLS`
  - `DGS10`
  - `DTWEXBGS`
  - `DFF`

说明：

- 当前机器上 FRED 抓取支持：
  - `reqwest` 主路径
  - Windows 下 `curl.exe` fallback
- **当前版本更新**：FRED 获取改为可配置开关（`config/fred.toml`）
  - `enabled = true`：通过 FRED API 获取（`api.stlouisfed.org/fred`）
  - `enabled = false`：使用 ClickHouse 已存宏观数据，跳过 HTTP 获取
  - API Key 在 `config/fred.toml` 中管理，支持环境变量插值（`${FRED_API_KEY}`）
  - 限流：500ms 请求间隔，遵守 FRED API 120 次/分钟限制
  - 无配置时默认 `enabled = true`，但缺失 API Key 会提示用户创建配置文件
- CLI 命令：`set-fred-config` / `show-fred-config`

---

## 4. 数据口径

### 4.1 日线价格口径

当前系统统一使用：

- Eastmoney：`fqt=1`
- Tencent：`qfq`

也就是：

> **统一前复权日线口径**

### 4.2 配置元数据口径

`config/universe.json` 中：

- `symbol`：系统主标识
- `display_symbol`：展示标识
- `eastmoney_secid / tencent_symbol`：抓数标识

即：

- 展示层元数据 ≠ provider 层元数据

---

## 5. 核心数据流

### 5.1 行情采集与存储

入口：

- `AppContext::ingest_daily`

流程：

1. 从 `config/universe.json` 读取标的池
2. 对每个标的调用 `fetch_daily_bars`
3. `fetch_daily_bars` 内部按顺序：
   - Eastmoney
   - 失败时 Tencent fallback
4. 每根 bar 进入 `normalize_daily_bar()`
5. 持久化进入 ClickHouse `daily_bar`

输出表：

- `instrument`
- `daily_bar`

### 5.2 指标计算流

入口：

- `AppContext::compute_indicators`

输入：

- `daily_bar`

处理：

- `MA10 / 20 / 30 / 60 / 120`
- `EMA12 / EMA26`
- `MACD / signal / hist`
- `RSI14`
- `ATR14`
- `VOL_MA20 / VOL_MA60`

输出表：

- `indicator_snapshot`

### 5.3 宏观与 regime 计算流

入口：

- `AppContext::compute_macro_regime`

输入：

- FRED 因子序列
- CN anchor：`000300`
- HK anchor：`HSI`

处理分三层：

#### 第一层：`macro_snapshot`

把宏观因子转成：

- `factor_value`
- `factor_score`

#### 第二层：`market_regime`

结合：

- 宏观因子
- CN / HK anchor 趋势打分

输出为真正的 per-scope regime：

- `GLOBAL`
- `CN`
- `HK`

每个 scope 独立产出：

- `trend_score`
- `liquidity_score`
- `risk_score`
- `regime_label`
- `macro_as_of_date`

#### 第三层：`environment_snapshot`

结合：

- scoped regime
- watchlist breadth proxy
- volume / turnover participation proxy

输出：

- `breadth_pct`
- `breadth_pct_sma5`
- `breadth_5d_delta`
- `breadth_state`
- `liquidity_proxy_score`
- `stress_proxy_score`
- `environment_score`
- `environment_label`

### 当前 regime 特性

系统已经改成：

> **宏观因子按最近可用值向后延用（forward-fill / as-of）**

为保证这个语义在你手动跑窄窗口时也成立，`compute_macro_regime(from, to)` 当前会：

1. 向前多取一段宏观历史（lookback buffer）
2. 在这段完整历史上构建 `macro_snapshot / market_regime / environment_snapshot`
3. 最后只把 `[from, to]` 区间内的结果写回数据库

所以：

- `report_date` 可以是最新分析日
- `regime_as_of_date` 保留真实宏观参考日期
- `regime_stale_days` 显示滞后天数

### 运行期容错语义

`compute_macro_regime(from, to)` 现在不是“本次外部抓取失败就直接清空该窗口宏观层”，而是：

1. 先尝试抓取最新 FRED 因子
2. 把成功抓到的因子写回 `macro_snapshot`
3. 同时读取库里已持久化的 `macro_snapshot` 历史
4. 用“已持久化历史 + 本次成功抓到的新因子”一起重建 `market_regime / environment_snapshot`

这意味着：

- 某个 FRED 因子短时异常时，系统仍可用最近一次成功写入的宏观历史做 as-of forward-fill
- 只有当某个宏观分组（风险组 / 流动性组）完全没有任何可用历史时，才会退化到更保守的中性分数
- provider 异常会进入 `failed_items`，但不会再静默把 scoped regime / environment 整段打空

输出表：

- `macro_snapshot`
- `market_regime`
- `environment_snapshot`

### 5.4 轮动计算流

入口：

- `AppContext::compute_rotation`

输入：

- `daily_bar`

处理：

- 计算 `RS20 / RS60 / RS120`
- 合成 `momentum_score`
- 按交易日横截面排名

输出表：

- `rotation_rank`

Dashboard 展示：

- `Leaders`
- `Laggards`

### 5.5 策略偏好计算流

入口：

- `AppContext::compute_strategy_preferences`

输入：

- `daily_bar`
- `indicator_snapshot`
- `market_regime`
- `rotation_rank`

当前语义：

- 这一层当前会按 `GLOBAL / CN / HK` 构建对应分析上下文
- 输出会显式携带 `analysis_scope` 与 `regime_basis_scope`

当前四类策略：

- `VALUE_LEFT`
- `TREND_PULLBACK`
- `TREND_BREAKOUT`
- `MOMENTUM_RIGHT`

输出表：

- `strategy_preference`

### 5.6 最终信号计算流

入口：

- `AppContext::compute_signals`

输入：

- `strategy_preference`
- `market_regime`
- `rotation_rank`

当前语义：

- `signal_snapshot` 当前会显式携带 `analysis_scope` 与 `regime_basis_scope`
- dashboard / report / frontend 需要结合这些 provenance 一起解释 signal，而不是只根据当前 dashboard scope 直接推断

输出：

- `final_score`
- `signal_label`
- `explanation`

输出表：

- `signal_snapshot`

Dashboard 展示：

- `Bullish opportunities`
- `Defensive / sell watch`

### 5.7 回测流

入口：

- `AppContext::run_backtest`

输入：

- `signal_snapshot`
- `daily_bar`

处理方式：

- 多头组合
- 次日开盘成交
- 手续费 / 滑点
- 最大持仓数约束

输出表：

- `backtest_run`
- `backtest_trade`
- `backtest_equity_curve`

当前用户面语义：

- 最新 backtest 会暴露 `analysis_scope`、`signal_scope`、`regime_basis_scope`
- dashboard / report 还会显式标出它是否 `matches current snapshot`

### 5.8 报告与 Dashboard 流

入口：

- `AppContext::dashboard_snapshot`
- `AppContext::export_report`
- `AppContext::export_data_health_report`

输入：

- `market_regime`
- `environment_snapshot`
- `rotation_rank`
- `signal_snapshot`
- `backtest_run`
- `data_health_summary`

输出：

- `DashboardSnapshot`
- `PipelineDateDiagnostics`
- Markdown 日报
- Markdown 数据健康报告

输出表 / 文件：

- `report_snapshot`
- `reports/daily-report-*.md`
- `reports/data-health-*.md`

其中：

- `daily-report-*.md` 的日期对应 `report_date`
- `data-health-*.md` 的日期对应 **本次检查覆盖到的 freshest market date**，不是导出瞬间的 wall-clock 日期

### 5.9 V3 sync-and-export 流（一键同步导出）

入口：

- `AppContext::sync_and_export`

流程：

1. 若显式传了 `--date`，跳过自动刷新，直接导出该日期报告
2. 若未传 `--date`：
   - 调用 `explain_latest_gate(scope)` 检查 gate 状态
   - 若 gate 已推进到最新，直接导出
   - 若 gate 落后，自动调用 `refresh_pipeline()` 进行全链路刷新
   - 刷新完成后再次检查 gate
   - 若 gate 仍落后，fail-loud 报错退出
   - 若 gate 推进成功，导出报告
3. 返回 JSON 包含报告路径、刷新状态、gate 信息

说明：

- 这是 V3 新增的**推荐默认 CLI 路径**
- 一条命令完成：检查 → 刷新（如需要）→ 导出
- Progress 输出到 stderr，stdout 保持纯 JSON

### 5.10 V3 LLM 分析流（大模型报告分析）

入口：

- `AppContext::analyze_report_with_llm`

流程：

1. 从 SQLite `app_config` 读取 LLM 配置（base_url / model / timeout）
2. 从 OS keyring 或 SQLite `credential_store` 读取 API Key
3. 通过 `dashboard_snapshot_with_scope()` 获取快照，用 `render_markdown_report()` 生成报告 Markdown
4. 构建 prompt：system 消息（量化分析师角色）+ 报告内容 + 请求分析解读
5. 调用 OpenAI-compatible API（同步阻塞）
6. 保存响应到 `reports/llm-analysis-{scope}-{date}.md`
7. 注册到 `report_snapshot` 表，可被桌面端 Recent Reports 识别

安全约束：

- API Key 绝不出现在 stdout、stderr、日志、错误信息、JSON 输出或测试快照中
- 前端/桌面端不接触 API Key，仅在 Rust CLI / app-service 层处理
- API Key 优先存储在 OS keyring，keyring 不可用时回退到 SQLite（明文，有警告提示）

---

## 6. 桌面端刷新逻辑

当前 `Refresh data` 已改成：

> **后台异步执行 + 前端进度条 + 按钮禁用防重入**

后台步骤：

1. 计算刷新窗口
2. `ingest_daily`
3. `compute_indicators`
4. `compute_macro_regime`
5. `compute_rotation`
6. `compute_strategy_preferences`
7. `compute_signals`

前端行为：

- 显示阶段进度
- Refresh 按钮置灰
- 主 dashboard 先回填
- `Data health` 再异步补载
- refresh 成功后，以 `trust summary` 作为主可信度入口；`Pipeline freshness` 与 `Data health` 继续作为证据层

---

## 7. 关键日期语义

### `Selected analysis date`

当前正在查看的分析日期。

### `Latest available analysis`

当前系统可供选择的最新分析日期。

它本质上由下游可用链路共同决定，当前至少要求：

- `signal_snapshot` 有数据
- `rotation_rank` 同日有数据
- `market_regime` 存在不晚于该日的 regime
- `environment_snapshot` 存在不晚于该日的 environment

### `Regime as-of date`

当前 dashboard 实际采用的宏观环境日期。

### `Scope`

当前 dashboard / report 的观察视角：

- `GLOBAL`
- `CN`
- `HK`

需要注意：

> `dashboard/report` 当前已按 scope 读取 regime / environment；signal / backtest 则需要继续结合它们自己的 provenance（如 `analysis_scope` / `regime_basis_scope` / `matches current snapshot`）一起理解，而不是只按当前 dashboard scope 直接推断。

### `regime_stale_days`

分析日期与宏观环境日期之间的滞后天数。

这通常表示：

- 行情 / 指标 / 信号已经更新到最新交易日
- 但宏观观测值本身的发布时间更慢

因此：

> `report_date` 新，不代表 `regime_as_of_date` 也必须同样新。

### `Pipeline freshness`

这是当前用于排查“日期是否到了、最新日是否完整”的第一诊断层。

它会同时给出：

- `latest_date`
- `lag_days`
- `is_latest`
- `latest_entities`
- `expected_entities`
- `is_complete`

需要注意：

> **`is_complete` 不能单独看，必须和 `is_latest` / `lag_days` 一起看。**

因为一个 stage 可能：

- 对它自己的 `latest_date` 来说是完整的
- 但它整体仍然落后于 freshest market date

---

## 8. 数据健康检查流

入口：

- `AppContext::check_data_health`

当前检查项：

- 主源可达性
- fallback 可达性
- 缺口
- 异常大波动
- 缺失 turnover
- FRED 宏观源 transport（`reqwest` / `curl` / `failed`）
- freshest market date 的 symbol 覆盖情况

输出：

- `DataHealthSummary`
- Markdown 报告
- Dashboard 数据健康面板

当前新增字段：

- `freshest_market_date`
- `symbols_on_freshest_market_date`
- `symbols_missing_freshest_market_date`
- `freshest_market_date_complete`

这层检查回答的是：

> **最新市场日期这一天，标的池是不是全量落库了。**

它和 `Pipeline freshness` 的区别是：

- `Pipeline freshness` 更偏 stage 级别
- `Data health` 更偏 symbol/provider 级别

---

## 9. 一句话概括

> **这是一个以 ClickHouse 为分析底座、以 Rust engine 为处理核心、以 Tauri + Vue 3 为展示容器的本地量化研究系统，支持中英双语国际化界面。**

它的核心价值是：

- 把多来源数据统一到一条分析链中
- 把原始行情加工成可解释的研究结果
- 把健康检查、信号、回测、报告、UI 放在同一个闭环中

---

## 10. V4 桌面端 LLM 智能分析流

V4 在原有数据管线之上新增了**大模型智能分析层**，让用户不需要输入任何 CLI 命令，即可在桌面端获得 AI 视角的量化分析报告。

### 10.1 整体数据流

```
┌─────────────────────────────────────────────────────────────────┐
│  前端 Vue 3                                                     │
│  ├─ LlmAnalysisTrigger.vue（顶部「智能分析」按钮）               │
│  ├─ LlmAnalysisPanel.vue（6 标签页面板）                         │
│  ├─ AgentProfileEditor.vue（YAML 实时编辑器）                    │
│  └─ SkillRouterPanel.vue（技能路由可视化）                       │
└───────────────────────┬─────────────────────────────────────────┘
                        │  Tauri invoke()
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  apps/desktop/src-tauri                                         │
│  ├─ get_llm_status                                              │
│  ├─ list_agent_profiles                                         │
│  ├─ list_skills                                                 │
│  ├─ analyze_with_skill                                          │
│  ├─ evaluate_skill_triggers                                     │
│  ├─ save_agent_profile                                          │
│  └─ read_agent_profile                                          │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  crates/app-service（AppContext）                                │
│  ├─ analyze_with_skill()                                        │
│  │   └─ 读取 dashboard_snapshot + 构建 ResearchContext          │
│  │   └─ 调用 research-skills::analyze()                         │
│  ├─ evaluate_skill_triggers()                                   │
│  │   └─ 针对当前 ResearchContext 评估所有技能触发条件            │
│  ├─ export_llm_analysis()                                       │
│  └─ save_agent_profile() / read_agent_profile()                │
└───────────────────────┬─────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  crates/research-skills                                          │
│  ├─ SkillRegistry（加载 skills/*.md，解析 YAML front matter）   │
│  ├─ AgentProfile（system prompt、参数、技能绑定）                │
│  ├─ 技能路由（calculate_weight → 按条件排序）                   │
│  └─ LLM Provider（OpenAiProvider / PlaceholderProvider）        │
└───────────────────────┬─────────────────────────────────────────┘
                        │  HTTP(S)
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│  OpenAI-compatible API                                          │
│  （base_url + model + api_key）                                  │
└─────────────────────────────────────────────────────────────────┘
                        │
                        ▼
返回结构化分析结果 → 前端 6 标签页渲染
```

### 10.2 关键组件职责

| 层级 | 组件 | 职责 |
|------|------|------|
| 前端 | `LlmAnalysisTrigger` | Dashboard 顶部入口按钮，集成 Agent/Skill 下拉选择器 |
| 前端 | `LlmAnalysisPanel` | 6 标签页容器：分析结果、技能路由、导出、配置、历史、比较 |
| 前端 | `SkillRouterPanel` | 可视化展示触发/未触发技能及其权重 |
| 前端 | `AgentProfileEditor` | YAML 实时编辑，语法高亮，保存后立即生效 |
| Tauri | `get_llm_status` | 返回 LLM 配置状态（是否已配置、provider、model） |
| Tauri | `analyze_with_skill` | 核心分析命令：Agent + Skill + Dashboard snapshot → LLM |
| Tauri | `evaluate_skill_triggers` | 评估当前 dashboard 条件下哪些技能会被触发 |
| Tauri | `save/read_agent_profile` | Agent YAML 配置的持久化 |
| AppContext | `analyze_with_skill()` | 编排：加载 snapshot → 构建 ResearchContext → 调用 research-skills |
| AppContext | `evaluate_skill_triggers()` | 遍历 SkillRegistry，针对当前 ResearchContext 评估每个技能的触发条件 |
| research-skills | `SkillRegistry` | 从 `crates/research-skills/skills/` 加载 SKILL.md，解析 YAML front matter |
| research-skills | `AgentProfile` | 绑定 system prompt、参数和可用技能列表 |
| research-skills | `calculate_weight()` | 根据 ResearchContext 的条件匹配度计算技能权重 |
| research-skills | `OpenAiProvider` | 真实 LLM 调用（reqwest + async-openai） |
| research-skills | `PlaceholderProvider` | 未配置 API Key 时的降级提供者，返回示例分析 |

### 10.3 安全设计

| 层面 | 措施 |
|------|------|
| API Key 存储 | 优先 OS keyring → 回退 SQLite `credential_store`（明文，带警告） |
| 传输安全 | 仅支持 HTTPS base_url |
| XSS 防御 | 三层过滤：HTML escape → marked 转换 → DOMPurify 标签过滤 |
| 降级策略 | 无 Key 时自动使用 PlaceholderProvider，UI 显示黄色提示条 |
| 信息泄露防护 | API Key 不出现在 stdout、stderr、log、前端网络请求中 |

### 10.4 与 V3 CLI LLM 分析的区别

| 维度 | V3 CLI | V4 桌面端 |
|------|--------|----------|
| 入口 | `cargo run -p quant-cli -- analyze-with-llm` | Dashboard 顶部「智能分析」按钮 |
| 交互 | 命令行参数 | UI 下拉选择 Agent + Skill |
| 技能路由 | 无 | 自动评估 + 可视化展示 |
| 历史记录 | 文件系统 | localStorage 缓存 50 条 |
| 比较分析 | 无 | 并排比较两个 Agent 结果 |
| Agent 编辑 | 无 | 内置 YAML 实时编辑器 |
| 导出 | 自动生成文件 | 一键导出 + 注册到 Recent reports |
